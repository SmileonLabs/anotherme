CREATE TABLE IF NOT EXISTS "fan_broadcasts" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "community_id" uuid NOT NULL REFERENCES "public"."fan_communities"("id") ON DELETE CASCADE, "author_user_id" uuid NOT NULL REFERENCES "public"."users"("id") ON DELETE CASCADE, "title" text NOT NULL, "body" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now());--> statement-breakpoint
CREATE TABLE IF NOT EXISTS "fan_missions" ("id" uuid PRIMARY KEY DEFAULT gen_random_uuid(), "community_id" uuid NOT NULL REFERENCES "public"."fan_communities"("id") ON DELETE CASCADE, "title" text NOT NULL, "description" text NOT NULL, "status" text NOT NULL DEFAULT 'ACTIVE', "created_at" timestamptz NOT NULL DEFAULT now());--> statement-breakpoint
CREATE TABLE IF NOT EXISTS "fan_mission_participants" ("mission_id" uuid NOT NULL REFERENCES "public"."fan_missions"("id") ON DELETE CASCADE, "user_id" uuid NOT NULL REFERENCES "public"."users"("id") ON DELETE CASCADE, "status" text NOT NULL DEFAULT 'JOINED', "joined_at" timestamptz NOT NULL DEFAULT now());--> statement-breakpoint
CREATE UNIQUE INDEX IF NOT EXISTS "fan_mission_participants_unique_idx" ON "fan_mission_participants" USING btree ("mission_id", "user_id");
