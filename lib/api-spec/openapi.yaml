openapi: 3.1.0
info:
  # Do not change the title, if the title changes, the import paths will be broken
  title: Api
  version: 0.1.0
  description: TodoTalk messenger API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: users
    description: User profile operations
  - name: friends
    description: Friend management
  - name: rooms
    description: Chat rooms
  - name: messages
    description: Chat messages
  - name: invites
    description: Invite links
  - name: blocked
    description: Block management
  - name: calls
    description: Voice calls
  - name: realtime
    description: Realtime connection setup
  - name: quests
    description: Daily/weekly quests & achievements
  - name: nft
    description: Published NFT collection and ownership operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
        "503":
          description: A required dependency is unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"

  /realtime/ticket:
    post:
      operationId: createRealtimeTicket
      tags: [realtime]
      summary: Create a one-time ticket for a realtime WebSocket connection
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Short-lived single-use connection ticket
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RealtimeTicket"
        "401":
          description: Unauthorized
        "403":
          description: Origin not allowed
        "503":
          description: Realtime service unavailable

  /users:
    get:
      operationId: listUsers
      tags: [users]
      summary: List all registered users
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: All registered users (excluding me)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /search:
    get:
      operationId: globalSearch
      tags: [users]
      summary: Search public profiles, STAR/FAN identities, public feed posts, and missions
      security: [{ bearerAuth: [] }]
      parameters:
        - in: query
          name: q
          required: true
          schema: { type: string, minLength: 2, maxLength: 80 }
        - in: query
          name: type
          schema: { type: string, enum: [all, users, stars, fans, posts, missions], default: all }
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 30, default: 20 }
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GlobalSearchResponse"
        "400": { description: Invalid search query }
        "401": { description: Unauthorized }

  /search/recommendations:
    get:
      operationId: getSearchRecommendations
      tags: [users]
      summary: Get recommended public STAR and FAN character profiles
      security: [{ bearerAuth: [] }]
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 4, maximum: 30, default: 12 }
      responses:
        "200":
          description: Recommended profiles for the active character profile
          content:
            application/json:
              schema:
                type: object
                required: [items]
                properties:
                  items:
                    type: array
                    items:
                      $ref: "#/components/schemas/SearchRecommendation"
        "400": { description: Invalid recommendation request }
        "401": { description: Unauthorized }

  /search/trending:
    get:
      operationId: getTrendingSearches
      tags: [users]
      summary: Get cached popular search terms
      security: [{ bearerAuth: [] }]
      parameters:
        - in: query
          name: limit
          schema: { type: integer, minimum: 1, maximum: 10, default: 5 }
      responses:
        "200":
          description: Popular search terms
          content:
            application/json:
              schema:
                type: object
                required: [items, generatedAt]
                properties:
                  items:
                    type: array
                    items:
                      type: object
                      required: [term, rank, change, resultCount]
                      properties:
                        term: { type: string }
                        rank: { type: integer }
                        change: { type: integer }
                        resultCount: { type: integer }
                  generatedAt: { type: string, format: date-time }
        "401": { description: Unauthorized }

  /search/admin/blocks:
    get:
      operationId: listSearchTrendingBlocks
      tags: [users]
      summary: List hidden trending search terms
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Hidden terms
          content:
            application/json:
              schema:
                type: array
                items:
                  type: object
                  required: [normalizedTerm, reason, createdAt]
                  properties:
                    normalizedTerm: { type: string }
                    reason: { type: string }
                    createdBy: { type: [string, "null"], format: uuid }
                    createdAt: { type: string, format: date-time }
        "403": { description: Admin required }
    post:
      operationId: blockSearchTrendingTerm
      tags: [users]
      summary: Hide a term from trending search results
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [term]
              properties:
                term: { type: string, minLength: 2, maxLength: 80 }
                reason: { type: string, maxLength: 200 }
      responses:
        "201": { description: Term blocked }
        "400": { description: Invalid term }
        "403": { description: Admin required }

  /search/admin/blocks/{term}:
    delete:
      operationId: unblockSearchTrendingTerm
      tags: [users]
      summary: Remove a term from the trending block list
      security: [{ bearerAuth: [] }]
      parameters:
        - in: path
          name: term
          required: true
          schema: { type: string }
      responses:
        "200": { description: Term unblocked }
        "403": { description: Admin required }

  /users/me:
    get:
      operationId: getMe
      tags: [users]
      summary: Get my profile
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: My profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
        "401":
          description: Unauthorized
    patch:
      operationId: updateMe
      tags: [users]
      summary: Update my profile
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserProfileUpdate"
      responses:
        "200":
          description: Updated profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"
    delete:
      operationId: deleteMe
      tags: [users]
      summary: Delete my account
      security: [{ bearerAuth: [] }]
      responses:
        "204":
          description: Deleted

  /users/me/profile-history:
    get:
      operationId: listMyProfileHistory
      tags: [users]
      summary: List my profile update history
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Profile update history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ProfileUpdateHistoryItem"

  /users/me/push-token:
    post:
      operationId: registerPushToken
      tags: [users]
      summary: Register push token
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PushTokenInput"
      responses:
        "200":
          description: Token registered
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserProfile"

  /users/me/persona:
    get:
      operationId: getMyPersona
      tags: [users]
      summary: Get my Another Me persona
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: My persona
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaProfile"
        "401":
          description: Unauthorized

  /users/me/persona/card:
    get:
      operationId: getMyPersonaCard
      tags: [users]
      summary: Get my Another Me identity card
      description: >
        Ontology-only Another Me identity/sync view. Returns 404 until an
        ontology snapshot exists. No legacy stats fallback, AI call, or XP/stat
        mutation.
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: My persona identity card
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaCard"
        "404":
          description: Persona ontology profile has not been created yet
        "401":
          description: Unauthorized

  /users/me/persona/analyze:
    post:
      operationId: analyzeMyPersona
      tags: [users]
      summary: Run an on-demand AI analysis of my Another Me persona
      description: >
        Triggered only by the user. Analyzes recent app activity and enqueues
        ontology evidence without storing legacy AI detail fields. Rate-limited
        to once per 10 minutes per user.
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Persona state after enqueueing ontology analysis evidence
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaProfile"
        "401":
          description: Unauthorized
        "422":
          description: Not enough activity to analyze
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaAnalysisError"
        "429":
          description: Cooldown — analysis was run too recently
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaAnalysisError"
        "502":
          description: AI analysis failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaAnalysisError"
        "503":
          description: AI analysis temporarily unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaAnalysisError"

  /users/persona/rankings:
    get:
      operationId: getPersonaRankings
      tags: [users]
      summary: Get persona leaderboards
      description: >
        Read-only cumulative leaderboards derived from existing persona stats. No
        AI call and no XP/stat mutation. Only non-sensitive fields are returned
        (name, avatar, level, title, archetype, score, primary stat).
      security: [{ bearerAuth: [] }]
      parameters:
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [overall, persuasion, logic, empathy, strategy, archetype]
            default: overall
        - name: archetype
          in: query
          required: false
          description: Only used when type=archetype.
          schema:
            type: string
            enum:
              [
                strategist,
                harmonizer,
                explorer,
                pioneer,
                sage,
                entertainer,
                activist,
                observer,
              ]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Ranking list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PersonaRanking"
        "401":
          description: Unauthorized

  /users/me/clan:
    get:
      operationId: getMyClan
      tags: [clans]
      summary: Get my clan (or null)
      description: >
        My clan with my role, member count, and a few recent members. Returns
        null when I am not in any clan. Member rows expose only non-sensitive
        fields (name, avatar, level, title, archetype, role, contribution).
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: My clan or null
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/MyClan"
                  - type: "null"
        "401":
          description: Unauthorized

  /clans:
    get:
      operationId: listClans
      tags: [clans]
      summary: Browse / search clans
      security: [{ bearerAuth: [] }]
      parameters:
        - name: q
          in: query
          required: false
          description: Search term matched against name and description.
          schema:
            type: string
        - name: archetype
          in: query
          required: false
          schema:
            type: string
            enum:
              [
                strategist,
                harmonizer,
                explorer,
                pioneer,
                sage,
                entertainer,
                activist,
                observer,
              ]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 30
        - name: scope
          in: query
          required: false
          schema:
            type: string
            enum: [recommended, following]
            default: recommended
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Clan list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClanSummary"
        "401":
          description: Unauthorized
    post:
      operationId: createClan
      tags: [clans]
      summary: Create a clan
      description: >
        Create a clan and join it as owner. Fails if the user already belongs to
        a clan or the name is taken.
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClanCreate"
      responses:
        "201":
          description: Created clan (my-clan view)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyClan"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "401":
          description: Unauthorized
        "409":
          description: Already in a clan, or name taken
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/rankings:
    get:
      operationId: getClanRankings
      tags: [clans]
      summary: Get clan leaderboards
      description: >
        Read-only clan leaderboards. No AI call and no XP / clan-EXP mutation.
        Only non-sensitive clan-level fields are returned (name, emblem, level,
        exp, member count, dominant archetype, clan power, strengths, score) —
        never any per-member PII.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [overall, level, contribution, average_level, archetype]
            default: overall
        - name: archetype
          in: query
          required: false
          description: Only used when type=archetype.
          schema:
            type: string
            enum:
              [
                strategist,
                harmonizer,
                explorer,
                pioneer,
                sage,
                entertainer,
                activist,
                observer,
              ]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 50
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Clan ranking list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanRanking"
        "401":
          description: Unauthorized

  /clans/{id}:
    get:
      operationId: getClanDetail
      tags: [clans]
      summary: Get clan detail with members
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Clan detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanDetail"
        "401":
          description: Unauthorized
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/{id}/identity:
    get:
      operationId: getClanIdentity
      tags: [clans]
      summary: Get a clan's collective identity (computed, read-only)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Clan identity
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanIdentity"
        "401":
          description: Unauthorized
        "404":
          description: Not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/{id}/wisdom:
    get:
      operationId: getClanWisdom
      tags: [clans]
      summary: Get a clan's collective wisdom (members only)
      description: >
        Returns a clan's AI-summarized collective wisdom (philosophy / strategy /
        values / culture / motto), derived read-only from existing clan memories
        and collective identity. Members only. Returns null when no wisdom has
        been generated yet. No XP / clan-EXP / ranking mutation.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Clan wisdom, or null if not generated yet
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/ClanWisdom"
                  - type: "null"
        "401":
          description: Unauthorized
        "403":
          description: Not a clan member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: Clan not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/{id}/wisdom/generate:
    post:
      operationId: generateClanWisdom
      tags: [clans]
      summary: Generate (or regenerate) a clan's wisdom (owner/elder only)
      description: >
        Summarizes the clan's existing memories and collective identity into a
        five-part wisdom via a single AI call, then stores it (one row per clan).
        Owner/elder only; runs only on this explicit request, never in real time.
        Requires at least one clan memory. Never mutates memories, persona XP,
        clan EXP, or ranking.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Generated wisdom
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWisdom"
        "400":
          description: No memories to summarize, or generation failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "401":
          description: Unauthorized
        "403":
          description: Not an owner/elder of the clan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: Clan not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/{id}/join:
    post:
      operationId: joinClan
      tags: [clans]
      summary: Join a clan
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Joined (my-clan view)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyClan"
        "401":
          description: Unauthorized
        "404":
          description: Clan not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "409":
          description: Already in a clan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/{id}/leave:
    post:
      operationId: leaveClan
      tags: [clans]
      summary: Leave a clan (deletes it if owner is the last member)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Leave result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanLeaveResult"
        "401":
          description: Unauthorized
        "403":
          description: Owner must transfer ownership first, or not a member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: Clan not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/{id}/memories:
    get:
      operationId: listClanMemories
      tags: [clans]
      summary: List a clan's memories (members only)
      description: >
        Returns a clan's accumulated memories (strategy / lesson / value /
        achievement / warning). Members only. No AI call, no XP / clan-EXP
        mutation, and no raw chat / battle / dungeon content — only the
        user-authored title, summary, tags, and an optional source reference.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [strategy, lesson, value, achievement, warning]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            default: 30
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Clan memory list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanMemoryList"
        "401":
          description: Unauthorized
        "403":
          description: Not a clan member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: Clan not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
    post:
      operationId: createClanMemory
      tags: [clans]
      summary: Create a clan memory (members only)
      description: >
        Create a user-authored clan memory. Members only. The body must contain
        a human-written summary — raw conversation/battle/dungeon content must
        never be sent here.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ClanMemoryCreate"
      responses:
        "201":
          description: Created memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanMemory"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "401":
          description: Unauthorized
        "403":
          description: Not a clan member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: Clan not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clans/{id}/memories/{memoryId}:
    delete:
      operationId: deleteClanMemory
      tags: [clans]
      summary: Delete a clan memory (author or elder/owner)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: memoryId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Deleted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanMemoryDeleteResult"
        "401":
          description: Unauthorized
        "403":
          description: Not allowed to delete this memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: Memory not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /users/search:
    get:
      operationId: searchUsers
      tags: [users]
      summary: Find a user by exact email address
      security: [{ bearerAuth: [] }]
      parameters:
        - name: email
          in: query
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Search results
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /friends:
    get:
      operationId: listFriends
      tags: [friends]
      summary: List my friends
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Friends list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"

  /friends/{userId}/alias:
    patch:
      operationId: updateFriendAlias
      tags: [friends]
      summary: Update my saved name for a friend
      security: [{ bearerAuth: [] }]
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FriendAliasInput"
      responses:
        "200":
          description: Updated friend display user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PublicUser"
        "400":
          description: Invalid alias
        "404":
          description: Friend not found

  /friends/{userId}:
    delete:
      operationId: removeFriend
      tags: [friends]
      summary: Remove a friend
      security: [{ bearerAuth: [] }]
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Removed

  /friend-requests:
    post:
      operationId: sendFriendRequest
      tags: [friends]
      summary: Send a friend request
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FriendRequestInput"
      responses:
        "201":
          description: Request sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FriendRequest"
        "400":
          description: Invalid request

  /friend-requests/incoming:
    get:
      operationId: listIncomingFriendRequests
      tags: [friends]
      summary: List incoming friend requests
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Incoming requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FriendRequestWithUser"

  /friend-requests/outgoing:
    get:
      operationId: listOutgoingFriendRequests
      tags: [friends]
      summary: List outgoing friend requests
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Outgoing requests
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/FriendRequestWithUser"

  /friend-requests/{id}/accept:
    patch:
      operationId: acceptFriendRequest
      tags: [friends]
      summary: Accept a friend request
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FriendRequest"

  /friend-requests/{id}/reject:
    patch:
      operationId: rejectFriendRequest
      tags: [friends]
      summary: Reject a friend request
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Rejected
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FriendRequest"

  /rooms:
    get:
      operationId: listRooms
      tags: [rooms]
      summary: List chat rooms I'm in
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Room list
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ChatRoom"
    post:
      operationId: createRoom
      tags: [rooms]
      summary: Create a chat room
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RoomInput"
      responses:
        "201":
          description: Room created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRoom"

  /rooms/{id}:
    get:
      operationId: getRoom
      tags: [rooms]
      summary: Get room details
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Room details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRoom"

  /rooms/{id}/leave:
    post:
      operationId: leaveRoom
      tags: [rooms]
      summary: Leave a room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Left

  /rooms/{id}/mute:
    patch:
      operationId: toggleMuteRoom
      tags: [rooms]
      summary: Toggle mute for a room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MuteInput"
      responses:
        "200":
          description: Mute toggled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRoomMember"

  /rooms/{id}/members:
    get:
      operationId: listRoomMembers
      tags: [rooms]
      summary: List room members
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"
    post:
      operationId: inviteRoomMembers
      tags: [rooms]
      summary: Invite additional friends to a group room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InviteMembersInput"
      responses:
        "200":
          description: Updated room
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRoom"

  /rooms/{id}/messages:
    get:
      operationId: fetchRoomMessages
      tags: [messages]
      summary: List messages in a room using a room sequence cursor
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: beforeSeq
          in: query
          description: Return messages with roomSeq lower than this cursor.
          schema:
            type: integer
            minimum: 1
        - name: limit
          in: query
          description: Page size, from 1 through 100 (defaults to 50).
          schema:
            type: integer
            minimum: 1
            maximum: 100
      responses:
        "200":
          description: Messages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Message"
    post:
      operationId: sendMessage
      tags: [messages]
      summary: Send a message
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MessageInput"
      responses:
        "201":
          description: Message sent
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /rooms/{id}/messages/{messageId}/delete:
    post:
      operationId: deleteMessage
      tags: [messages]
      summary: Delete a message for me or everyone
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DeleteMessageInput"
      responses:
        "204":
          description: Deleted

  /rooms/{id}/messages/{messageId}/forward:
    post:
      operationId: forwardMessage
      tags: [messages]
      summary: Forward a message as a new message without origin attribution
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ForwardMessageInput"
      responses:
        "201":
          description: Forwarded message
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Message"

  /rooms/{id}/messages/{messageId}/sticker:
    post:
      operationId: addMessageStickerBadge
      tags: [messages]
      summary: Attach or replace my sticker badge on a message
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
        - name: messageId
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StickerBadgeInput"
      responses:
        "204":
          description: Sticker attached

  /rooms/{id}/pin:
    post:
      operationId: pinRoomMessage
      tags: [rooms]
      summary: Pin one message in a room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PinMessageInput"
      responses:
        "204":
          description: Pinned
    delete:
      operationId: unpinRoomMessage
      tags: [rooms]
      summary: Clear the room pin
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Unpinned

  /rooms/{id}/read:
    patch:
      operationId: markRoomRead
      tags: [messages]
      summary: Mark room as read
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReadInput"
      responses:
        "204":
          description: Marked read

  /rooms/{id}/typing:
    get:
      operationId: getTypingUsers
      tags: [messages]
      summary: List members currently typing in a room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Typing members (excluding the requester)
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"
    post:
      operationId: setTyping
      tags: [messages]
      summary: Signal that the current user is typing (heartbeat)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Recorded

  /invites:
    post:
      operationId: createInvite
      tags: [invites]
      summary: Create an invite link
      security: [{ bearerAuth: [] }]
      responses:
        "201":
          description: Invite created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Invite"

  /invites/redeem:
    post:
      operationId: redeemInvite
      tags: [invites]
      summary: Redeem an invite code
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RedeemInviteInput"
      responses:
        "200":
          description: Redeemed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/FriendRequest"
        "400":
          description: Invalid or expired invite

  /blocked:
    get:
      operationId: listBlocked
      tags: [blocked]
      summary: List blocked users
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Blocked users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicUser"
    post:
      operationId: blockUser
      tags: [blocked]
      summary: Block a user
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BlockInput"
      responses:
        "201":
          description: Blocked

  /blocked/{userId}:
    delete:
      operationId: unblockUser
      tags: [blocked]
      summary: Unblock a user
      security: [{ bearerAuth: [] }]
      parameters:
        - name: userId
          in: path
          required: true
          schema:
            type: string
      responses:
        "204":
          description: Unblocked

  /calls:
    post:
      operationId: createCall
      tags: [calls]
      summary: Start a voice or video call
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CreateCallInput"
      responses:
        "201":
          description: Call created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallSession"

  /calls/incoming:
    get:
      operationId: listIncomingCalls
      tags: [calls]
      summary: List incoming ringing calls for the current user
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Incoming calls
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/CallWithCaller"

  /calls/{id}:
    get:
      operationId: getCall
      tags: [calls]
      summary: Get call status
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Call
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Call"

  /calls/{id}/accept:
    post:
      operationId: acceptCall
      tags: [calls]
      summary: Accept an incoming call
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Call accepted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallSession"

  /calls/{id}/join:
    post:
      operationId: joinCall
      tags: [calls]
      summary: Join a call from the in-chat call card (caller or callee)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Joined
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CallSession"

  /calls/{id}/decline:
    post:
      operationId: declineCall
      tags: [calls]
      summary: Decline an incoming call
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Call declined
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Call"

  /calls/{id}/cancel:
    post:
      operationId: cancelCall
      tags: [calls]
      summary: Cancel an outgoing call before it is answered (caller only)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Call cancelled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Call"

  /calls/{id}/end:
    post:
      operationId: endCall
      tags: [calls]
      summary: End an active or ringing call
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Call ended
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Call"

  /storage/uploads/request-url:
    post:
      tags: [Storage]
      operationId: requestUploadUrl
      summary: Request a presigned URL for file upload
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UploadUrlRequest"
      responses:
        "200":
          description: Presigned upload URL generated.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UploadUrlResponse"
        "400":
          description: Missing or invalid required fields.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          description: Failed to generate upload URL.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
  /storage/media-url:
    post:
      tags: [Storage]
      operationId: requestMediaUrl
      summary: Issue a short-lived authorized URL for a private object
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/MediaUrlRequest"
      responses:
        "200":
          description: Short-lived media URL issued.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MediaUrlResponse"
        "400": { description: Invalid request. }
        "401": { description: Authentication required. }
        "404": { description: Object not found or inaccessible. }
        "429": { description: Too many requests. }
        "503": { description: Media access is temporarily unavailable. }

  /storage/objects/{path}:
    get:
      tags: [Storage]
      operationId: getStorageObject
      summary: Serve an object entity from PRIVATE_OBJECT_DIR
      description: Requires a valid short-lived media ticket in the `ticket` query parameter.
      parameters:
        - name: path
          in: path
          required: true
          description: A path relative to the private objects directory; may contain `/` separators.
          schema:
            type: string
      responses:
        "200":
          description: Object content streamed with correct Content-Type.
          content:
            "*/*":
              schema:
                type: string
                format: binary
        "404":
          description: Object not found.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          description: Failed to serve object.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /dungeons:
    post:
      operationId: createDungeon
      tags: [dungeons]
      summary: Create a dungeon (MUD game room with AI Dungeon Master)
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DungeonInput"
      responses:
        "201":
          description: Dungeon room created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRoom"

  /dungeons/{id}:
    get:
      operationId: getDungeonState
      tags: [dungeons]
      summary: Get the current party/game state of a dungeon
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Dungeon game state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DungeonState"

  /life-quests:
    post:
      operationId: createLifeQuest
      tags: [lifeQuests]
      summary: Start a Life Quest (AI generates the whole scenario once)
      security: [{ bearerAuth: [] }]
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LifeQuestCreateInput"
      responses:
        "201":
          description: Life Quest created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifeQuest"

  /life-quests/active:
    get:
      operationId: getActiveLifeQuest
      tags: [lifeQuests]
      summary: Get the caller's most recent active Life Quest (or null)
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Active Life Quest or null
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifeQuestActiveResponse"

  /life-quests/{id}:
    get:
      operationId: getLifeQuest
      tags: [lifeQuests]
      summary: Get a Life Quest by id
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Life Quest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifeQuest"

  /life-quests/{id}/choose:
    post:
      operationId: chooseLifeQuestOption
      tags: [lifeQuests]
      summary: Pick a choice for the current stage (applies stats + XP, no AI)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LifeQuestChooseInput"
      responses:
        "200":
          description: Choice applied
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifeQuestChooseResult"

  /life-quests/{id}/abandon:
    post:
      operationId: abandonLifeQuest
      tags: [lifeQuests]
      summary: Abandon an active Life Quest (small consolation XP)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Life Quest abandoned
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LifeQuest"

  /battles:
    post:
      operationId: createBattle
      tags: [battles]
      summary: Create a talk-battle room (AI-judged debate game)
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BattleCreateInput"
      responses:
        "201":
          description: Battle room created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatRoom"

  /battle-topics:
    post:
      operationId: suggestBattleTopics
      tags: [battles]
      summary: Get AI-suggested debate topics for a category
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BattleTopicInput"
      responses:
        "200":
          description: Suggested topics
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleTopicSuggestions"

  /battle-personas:
    get:
      operationId: listBattlePersonas
      tags: [battles]
      summary: List available AI opponent personas
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Available AI personas
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattlePersonaList"

  /battle-stats:
    get:
      operationId: getMyBattleStats
      tags: [battles]
      summary: My lifetime talk-battle stats (record, MP, level)
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: My battle stats
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/MyBattleStats"

  /battle-history:
    get:
      operationId: getMyBattleHistory
      tags: [battles]
      summary: My recent finished battles (dashboard carousel)
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Recent finished battles
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/BattleHistoryItem"

  /battles/{id}:
    get:
      operationId: getBattleState
      tags: [battles]
      summary: Get the current state of a talk-battle
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Battle game state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleState"

  /battles/{id}/ready:
    post:
      operationId: setBattleReady
      tags: [battles]
      summary: Mark yourself ready in the waiting room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Updated battle state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleState"

  /battles/{id}/start:
    post:
      operationId: startBattle
      tags: [battles]
      summary: Start the debate (assign sides, generate opening question)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Updated battle state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleState"

  /battles/{id}/turn:
    post:
      operationId: submitBattleTurn
      tags: [battles]
      summary: Submit your utterance for the current turn (AI evaluates it)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BattleTurnInput"
      responses:
        "200":
          description: Updated battle state after evaluation
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleState"

  /battles/{id}/restart:
    post:
      operationId: restartBattle
      tags: [battles]
      summary: Reset the battle back to the waiting room for another game
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Battle reset to waiting room
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleState"

  /battles/{id}/cancel:
    post:
      operationId: cancelBattle
      tags: [battles]
      summary: Cancel a waiting battle and delete its room (host only)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Battle cancelled and room deleted
          content:
            application/json:
              schema:
                type: object
                required: [ok]
                properties:
                  ok:
                    type: boolean

  /clan-wars:
    get:
      operationId: listClanWars
      tags: [clan-wars]
      summary: List clan wars relevant to the caller
      description: >
        Returns open public challenges plus all wars involving the caller's clan,
        most recent first. Optionally filtered by status.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: status
          in: query
          required: false
          schema:
            type: string
            enum: [open, matched, active, completing, completed, cancelled]
      responses:
        "200":
          description: List of clan war summaries
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ClanWarSummary"
        "401":
          description: Unauthorized
    post:
      operationId: createClanWar
      tags: [clan-wars]
      summary: Create a clan war (challenger owner/elder only)
      description: >
        The creator's clan becomes the challenger. Provide opponentClanId to
        directly challenge a clan (status=matched), or omit it for a public
        challenge (status=open) any other clan's owner/elder can accept. Owner or
        elder only.
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [topic]
              properties:
                topic:
                  type: string
                opponentClanId:
                  type: string
                  nullable: true
      responses:
        "201":
          description: The created war
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWarDetail"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "401":
          description: Unauthorized
        "403":
          description: Not an owner/elder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clan-wars/{id}:
    get:
      operationId: getClanWar
      tags: [clan-wars]
      summary: Get a clan war's detail
      description: >
        Open challenges are visible to everyone; other statuses require membership
        in one of the two participating clans. Never exposes other members' raw
        submissions — only the caller's own submission text and AI summaries.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: War detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWarDetail"
        "401":
          description: Unauthorized
        "403":
          description: Not allowed to view
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: War not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clan-wars/{id}/accept:
    post:
      operationId: acceptClanWar
      tags: [clan-wars]
      summary: Accept an open public challenge (opponent owner/elder)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The matched war
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWarDetail"
        "401":
          description: Unauthorized
        "403":
          description: Not an owner/elder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: War not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "409":
          description: Already matched or finished
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clan-wars/{id}/join:
    post:
      operationId: joinClanWar
      tags: [clan-wars]
      summary: Join a war as a member of a participating clan
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: War detail after joining
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWarDetail"
        "401":
          description: Unauthorized
        "403":
          description: Not a member of a participating clan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: War not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "409":
          description: War not joinable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clan-wars/{id}/submit:
    post:
      operationId: submitClanWarArgument
      tags: [clan-wars]
      summary: Submit a member's argument (once)
      description: >
        Auto-joins the caller if not already a participant. Each member may submit
        exactly once; a second submission is rejected.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [content]
              properties:
                content:
                  type: string
      responses:
        "200":
          description: War detail after submitting
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWarDetail"
        "400":
          description: Invalid input
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "401":
          description: Unauthorized
        "403":
          description: Not a member of a participating clan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: War not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "409":
          description: Already submitted, or war not accepting submissions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clan-wars/{id}/complete:
    post:
      operationId: completeClanWar
      tags: [clan-wars]
      summary: Judge and finalize a war (owner/elder of either clan)
      description: >
        Requires both sides to have at least one submission. Calls the AI judge
        exactly once to score every submission, derives each clan's score as the
        top-3 average, records the winner and result, and applies small isolated
        clan-EXP rewards. Idempotent — repeat calls return the completed war.
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The completed war
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWarDetail"
        "401":
          description: Unauthorized
        "403":
          description: Not an owner/elder of a participating clan
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: War not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "409":
          description: Not ready to complete
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "502":
          description: AI judge failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /clan-wars/{id}/cancel:
    post:
      operationId: cancelClanWar
      tags: [clan-wars]
      summary: Cancel a war before completion (challenger owner/elder)
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: The cancelled war
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanWarDetail"
        "401":
          description: Unauthorized
        "403":
          description: Not the challenger's owner/elder
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "404":
          description: War not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"
        "409":
          description: Already finished
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ClanError"

  /users/me/quests:
    get:
      operationId: getMyQuests
      tags: [quests]
      summary: Get my daily & weekly quests
      description: >
        Returns today's daily quests and this week's weekly quests. Progress is
        recomputed on read from existing activity (chat, battle, dungeon, clan,
        analysis); no new domain activity is created and no AI is called.
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: My quests for the current periods
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Quest"
        "401":
          description: Unauthorized

  /users/me/quests/{questKey}/claim:
    post:
      operationId: claimQuestReward
      tags: [quests]
      summary: Claim a completed quest's reward
      security: [{ bearerAuth: [] }]
      parameters:
        - name: questKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Reward granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardClaimResult"
        "401":
          description: Unauthorized
        "404":
          description: Unknown quest
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardClaimError"
        "409":
          description: Not completed or already claimed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardClaimError"

  /users/me/achievements:
    get:
      operationId: getMyAchievements
      tags: [quests]
      summary: Get my achievements
      description: >
        Returns all achievements with unlock/claim state. Unlock is computed on
        read from existing activity; no AI is called and no domain activity is
        created.
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: My achievements
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Achievement"
        "401":
          description: Unauthorized

  /users/me/achievements/{achievementKey}/claim:
    post:
      operationId: claimAchievementReward
      tags: [quests]
      summary: Claim an unlocked achievement's reward
      security: [{ bearerAuth: [] }]
      parameters:
        - name: achievementKey
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Reward granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardClaimResult"
        "401":
          description: Unauthorized
        "404":
          description: Unknown achievement
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardClaimError"
        "409":
          description: Not unlocked or already claimed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardClaimError"

  /users/me/rewards/summary:
    get:
      operationId: getMyRewardsSummary
      tags: [quests]
      summary: Count of claimable quest/achievement rewards (for badges)
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Claimable reward counts
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RewardsSummary"
        "401":
          description: Unauthorized

  /knowledge/memories:
    get:
      operationId: listKnowledgeMemories
      tags: [knowledge]
      summary: List my approved AI memories
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Approved memories, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/KnowledgeMemory"
    post:
      operationId: createKnowledgeMemory
      tags: [knowledge]
      summary: Create an approved manual AI memory
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KnowledgeMemoryCreate"
      responses:
        "201":
          description: Created memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeMemory"
        "400":
          description: Text is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /knowledge/memories/{id}:
    patch:
      operationId: updateKnowledgeMemory
      tags: [knowledge]
      summary: Update one of my AI memories
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/KnowledgeMemoryPatch"
      responses:
        "200":
          description: Updated memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeMemory"
        "404":
          description: Memory not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: archiveKnowledgeMemory
      tags: [knowledge]
      summary: Archive one of my AI memories
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Archived memory
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/KnowledgeMemory"
        "404":
          description: Memory not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /another-me/settings:
    get:
      operationId: getAnotherMeSettings
      tags: [another-me]
      summary: Get my Another Me settings
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Another Me settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeSettings"
    patch:
      operationId: updateAnotherMeSettings
      tags: [another-me]
      summary: Update my Another Me settings
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnotherMeSettingsPatch"
      responses:
        "200":
          description: Updated Another Me settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeSettings"
        "400":
          description: Invalid setting value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /another-me/rooms/{roomId}/settings:
    get:
      operationId: getAnotherMeRoomSettings
      tags: [another-me]
      summary: Get my Another Me settings for a room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: roomId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Room-specific Another Me settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeRoomSettings"
        "404":
          description: Room not found or caller is not a member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateAnotherMeRoomSettings
      tags: [another-me]
      summary: Update my Another Me settings for a room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: roomId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnotherMeRoomSettingsPatch"
      responses:
        "200":
          description: Updated room-specific Another Me settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeRoomSettings"
        "400":
          description: Invalid setting value
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Room not found or caller is not a member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /another-me/summon/status:
    get:
      operationId: getAnotherMeSummonStatus
      tags: [another-me]
      summary: Check whether an Another Me can be summoned into a direct room
      security: [{ bearerAuth: [] }]
      parameters:
        - name: roomId
          in: query
          required: true
          schema:
            type: string
            format: uuid
        - name: targetUserId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Summon eligibility and any active session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeSummonStatus"
        "400":
          description: Missing or invalid room or target user id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Room not found or caller is not a member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /another-me/summon:
    post:
      operationId: summonAnotherMe
      tags: [another-me]
      summary: Summon another participant's Another Me into a direct room
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnotherMeSummonInput"
      responses:
        "201":
          description: Active Another Me session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeSession"
        "400":
          description: Missing or invalid room or target user id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: The current relationship or room policy does not allow a summon
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Room not found or caller is not a member
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: The wait period is active or a session is already active
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /another-me/summon/{sessionId}/dismiss:
    post:
      operationId: dismissAnotherMeSession
      tags: [another-me]
      summary: Dismiss an Another Me session
      security: [{ bearerAuth: [] }]
      parameters:
        - name: sessionId
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Dismissed or already completed session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeSession"
        "403":
          description: Caller is not a party to the session
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Session not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /another-me/tone-profile/generate:
    post:
      operationId: generateAnotherMeToneProfile
      tags: [another-me]
      summary: Generate my Another Me tone profile from my messages
      security: [{ bearerAuth: [] }]
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AnotherMeToneProfileInput"
      responses:
        "200":
          description: Generated tone profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AnotherMeToneProfile"

  /daily-talk-reward/status:
    get:
      operationId: getDailyTalkRewardStatus
      tags: [daily-talk-reward]
      summary: Get my daily talk reward eligibility
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Current reward eligibility and streak
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyTalkRewardStatus"

  /daily-talk-reward/generate:
    post:
      operationId: generateDailyTalkReward
      tags: [daily-talk-reward]
      summary: Generate my AI daily talk diary draft
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Generated daily reward diary
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyTalkReward"
        "400":
          description: The reward is not ready to generate
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Talk analysis is disabled
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Reward already claimed or insufficient messages
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          description: AI generation is unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /daily-talk-reward/history:
    get:
      operationId: listDailyTalkRewardHistory
      tags: [daily-talk-reward]
      summary: List my daily talk reward history
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Daily talk rewards, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/DailyTalkReward"

  /daily-talk-reward/{id}:
    get:
      operationId: getDailyTalkReward
      tags: [daily-talk-reward]
      summary: Get one of my daily talk rewards
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Daily talk reward
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyTalkReward"
        "404":
          description: Reward not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    patch:
      operationId: updateDailyTalkReward
      tags: [daily-talk-reward]
      summary: Edit an unclaimed daily talk reward diary
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/DailyTalkRewardPatch"
      responses:
        "200":
          description: Updated daily talk reward
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyTalkReward"
        "400":
          description: Invalid diary fields
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Reward not found or already claimed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /daily-talk-reward/{id}/save:
    post:
      operationId: saveDailyTalkReward
      tags: [daily-talk-reward]
      summary: Save a daily talk reward and grant its PVT
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Reward saved and granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyTalkReward"
        "400":
          description: Diary has not been generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Reward not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Reward already granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /daily-talk-reward/{id}/post-to-feed:
    post:
      operationId: postDailyTalkRewardToFeed
      tags: [daily-talk-reward]
      summary: Post a daily talk reward diary to the STAR feed and grant its PVT
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Reward posted and granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DailyTalkReward"
        "400":
          description: Diary has not been generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Reward not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Reward already granted
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /official-accounts/bibi:
    get:
      operationId: getBibiOfficialProfile
      tags: [official-accounts]
      summary: Get the BIBI Official account profile and ensure the friendship exists
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: BIBI Official profile
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BibiOfficialProfile"

  /official-accounts/bibi/room:
    post:
      operationId: getOrCreateBibiOfficialRoom
      tags: [official-accounts]
      summary: Get or create my direct room with BIBI Official
      security: [{ bearerAuth: [] }]
      responses:
        "201":
          description: BIBI Official profile and direct room
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BibiOfficialRoom"

  /presence/heartbeat:
    post:
      operationId: recordPresenceHeartbeat
      tags: [presence]
      summary: Record my presence heartbeat
      security: [{ bearerAuth: [] }]
      requestBody:
        required: false
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PresenceHeartbeat"
      responses:
        "204":
          description: Presence recorded

  /presence/users:
    get:
      operationId: listPresenceUsers
      tags: [presence]
      summary: Get presence for users who share a room with me
      security: [{ bearerAuth: [] }]
      parameters:
        - name: ids
          in: query
          required: false
          description: Comma-separated user ids. Up to 50 ids are considered.
          schema:
            type: string
      responses:
        "200":
          description: Presence states for authorized requested users
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PresenceUsersResponse"

  /pvt/wallet:
    get:
      operationId: getPvtWallet
      tags: [pvt]
      summary: Get my PVT wallet balance
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: PVT wallet
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PvtWallet"

  /pvt/transactions:
    get:
      operationId: listPvtTransactions
      tags: [pvt]
      summary: List my PVT transactions
      security: [{ bearerAuth: [] }]
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: PVT transactions, newest first
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PvtTransaction"
        "400":
          description: Invalid limit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /star-feed/posts:
    get:
      operationId: listStarFeedPosts
      tags: [star-feed]
      summary: List STAR feed posts visible to me
      security: [{ bearerAuth: [] }]
      parameters:
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 20
        - name: scope
          in: query
          required: false
          schema:
            type: string
            enum: [recommended, following]
            default: recommended
        - name: cursor
          in: query
          required: false
          schema:
            type: string
            format: date-time
      responses:
        "200":
          description: Visible STAR feed posts, newest first
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarFeedPage"
        "400":
          description: Invalid limit
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    post:
      operationId: createStarFeedPost
      tags: [star-feed]
      summary: Create a FAN or official STAR feed post
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StarFeedPostInput"
      responses:
        "201":
          description: Created STAR feed post
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarFeedCreateResult"
        "400":
          description: Invalid post body
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: STAR post is not allowed for the current play state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlayModeError"
        "404":
          description: Target STAR profile was not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A STAR NFT must be equipped before posting as a STAR
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlayModeError"

  /star-feed/posts/{id}:
    get:
      operationId: getStarFeedPost
      tags: [star-feed]
      summary: Get one STAR feed post visible to me
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: STAR feed post detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarFeedPost"
        "400":
          description: Invalid post id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Post was not found or is not visible
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /star-feed/star-profiles/{id}/follow:
    post:
      operationId: followStarProfile
      tags: [star-feed]
      summary: Follow a public STAR profile
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Follow state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StarProfileFollowState" }
        "404":
          description: STAR profile not found
    delete:
      operationId: unfollowStarProfile
      tags: [star-feed]
      summary: Unfollow a STAR profile
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Follow state
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StarProfileFollowState" }
  /star-feed/posts/{id}/reactions:
    post:
      operationId: cheerStarFeedPost
      tags: [star-feed]
      summary: Add my cheer reaction to a STAR feed post
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: STAR feed post with the caller's reaction
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarFeedPost"
        "404":
          description: Post not found or not visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: uncheerStarFeedPost
      tags: [star-feed]
      summary: Remove my active profile reaction from a STAR feed post
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: STAR feed post after removing the reaction
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StarFeedPost" }
        "404":
          description: Post not found or not visible to the caller

  /star-feed/posts/{id}/comments:
    post:
      operationId: commentStarFeedPost
      tags: [star-feed]
      summary: Add a comment to a STAR feed post
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StarFeedCommentInput"
      responses:
        "201":
          description: STAR feed post with the new comment
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarFeedPost"
        "400":
          description: Invalid comment body
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Post not found or not visible to the caller
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /star-feed/posts/{id}/repost:
    post:
      operationId: repostStarFeedPost
      tags: [star-feed]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "201":
          description: Reposted feed post
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StarFeedPost" }
        "404": { description: Source post not found }

  /star-feed/posts/{id}/reports:
    post:
      operationId: reportStarFeedPost
      tags: [star-feed]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/StarFeedReportInput" }
      responses:
        "201": { description: Report accepted }

  /star-feed/discover:
    get:
      operationId: discoverStarFeedByHashtag
      tags: [star-feed]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: tag
          in: query
          required: true
          schema: { type: string }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100 }
      responses:
        "200":
          description: Public posts matching hashtag
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/StarFeedPost" }

  /star-feed/result-drafts:
    get:
      operationId: listStarResultDrafts
      tags: [star-feed]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Result drafts owned by the caller
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/StarResultDraft" }

  /star-feed/result-drafts/{id}/approve:
    post:
      operationId: approveStarResultDraft
      tags: [star-feed]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Published result card
          content:
            application/json:
              schema: { $ref: "#/components/schemas/StarFeedPost" }

  /star-feed/result-drafts/{id}/discard:
    post:
      operationId: discardStarResultDraft
      tags: [star-feed]
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Draft discarded }

  /fan-communities:
    get:
      operationId: listFanCommunities
      tags: [fan-community]
      security: [{ bearerAuth: [] }]
      responses:
        "200": { description: Active fan communities }
    post:
      operationId: createFanCommunity
      tags: [fan-community]
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json: { schema: { $ref: "#/components/schemas/FanCommunityInput" } }
      responses:
        "201": { description: Community created }

  /fan-communities/{id}/join:
    post:
      operationId: joinFanCommunity
      tags: [fan-community]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { "200": { description: Joined } }
    delete:
      operationId: leaveFanCommunity
      tags: [fan-community]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { "200": { description: Left } }

  /star-feed/admin/reports:
    get:
      operationId: listStarFeedReports
      tags: [star-feed-admin]
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Open reports
          content:
            application/json:
              schema:
                type: array
                items: { $ref: "#/components/schemas/StarFeedReport" }
        "403": { description: Admin access required }

  /star-feed/admin/reports/{id}/resolve:
    post:
      operationId: resolveStarFeedReport
      tags: [star-feed-admin]
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [action]
              properties:
                action: { type: string, enum: [keep, remove] }
      responses:
        "200": { description: Report resolved }
        "403": { description: Admin access required }

  /storage/uploads/object:
    post:
      operationId: uploadStorageObject
      tags: [Storage]
      summary: Upload raw object bytes through the API
      security: [{ bearerAuth: [] }]
      parameters:
        - name: name
          in: query
          required: false
          schema:
            type: string
        - name: size
          in: query
          required: true
          description: Declared size. The server stores the actual uploaded byte length.
          schema:
            type: integer
            minimum: 1
            maximum: 26214400
        - name: contentType
          in: query
          required: false
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/octet-stream:
            schema:
              type: string
              format: binary
      responses:
        "200":
          description: Stored object path and actual upload metadata
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ObjectUploadResponse"
        "400":
          description: Missing metadata, empty bytes, or an oversized upload
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          description: Upload failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /storage/public-objects/{filePath}:
    get:
      operationId: getPublicStorageObject
      tags: [Storage]
      summary: Serve an unconditionally public object
      parameters:
        - name: filePath
          in: path
          required: true
          description: A path relative to configured public object roots; may contain `/` separators.
          schema:
            type: string
      responses:
        "200":
          description: Public object content streamed with its stored Content-Type
          content:
            "*/*":
              schema:
                type: string
                format: binary
        "404":
          description: Public object not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"
        "500":
          description: Failed to serve the public object
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /users/me/play-mode:
    get:
      operationId: getMyPlayMode
      tags: [users]
      summary: Get my FAN or STAR play mode state
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Current play mode state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlayModeState"
    patch:
      operationId: updateMyPlayMode
      tags: [users]
      summary: Change my FAN or STAR play mode
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/PlayModeInput"
      responses:
        "200":
          description: Updated play mode state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlayModeState"
        "400":
          description: Mode must be fan or star
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: STAR mode is locked
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/PlayModeError"

  /users/me/profiles:
    get:
      operationId: getMyCharacterProfiles
      tags: [users]
      summary: List the account's activity profiles and active profile
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Character profile state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CharacterProfileState"

  /users/me/profile-context:
    get:
      operationId: getMyCharacterProfileContext
      tags: [users]
      summary: Resolve and verify the request's active character profile
      security: [{ bearerAuth: [] }]
      parameters:
        - in: header
          name: X-Character-Profile-Id
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Verified request actor
          content: { application/json: { schema: { $ref: "#/components/schemas/CharacterProfile" } } }
        "403": { description: Profile is not owned by the account }
        "409": { description: Profile is locked or unavailable }

  /users/me/fan-profiles:
    post:
      operationId: createMyFanCharacterProfile
      tags: [users]
      summary: Customize the initial FAN or create an unlocked additional FAN
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [displayName, customization]
              properties:
                displayName: { type: string, minLength: 1, maxLength: 30 }
                handle: { type: string, minLength: 3, maxLength: 24 }
                profileImageUrl: { type: [string, "null"], maxLength: 1024 }
                customizeDefault:
                  type: boolean
                  default: false
                  description: >
                    Onboarding-only flag. When true, an uncustomized default FAN
                    is completed in place. Ordinary FAN-add flows must omit it so
                    an existing profile is never overwritten.
                customization:
                  type: object
                  required: [ageStyle, hairStyle, skinTone, genderExpression]
                  properties:
                    ageStyle: { type: string }
                    hairStyle: { type: string }
                    skinTone: { type: string }
                    genderExpression: { type: string }
      responses:
        "201":
          description: Created and activated FAN
          content: { application/json: { schema: { $ref: "#/components/schemas/CharacterProfileState" } } }
        "409": { description: FAN expansion is not unlocked or handle is taken }

  /users/me/active-profile:
    patch:
      operationId: updateMyActiveCharacterProfile
      tags: [users]
      summary: Select the activity profile used by feed and chat
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [profileId]
              properties:
                profileId:
                  type: string
                  format: uuid
      responses:
        "200":
          description: Updated character profile state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CharacterProfileState"
        "404":
          description: Profile not owned by the account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Profile is locked or unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/profiles/{profileId}:
    patch:
      operationId: updateMyCharacterProfile
      tags: [users]
      summary: Update one owned activity profile
      security: [{ bearerAuth: [] }]
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              minProperties: 1
              properties:
                displayName:
                  type: string
                  minLength: 1
                  maxLength: 30
                profileImageUrl:
                  type: ["string", "null"]
                  maxLength: 1024
                statusMessage:
                  type: ["string", "null"]
                  maxLength: 100
      responses:
        "200":
          description: Updated profile state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CharacterProfileState"
        "400":
          description: Invalid profile update
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Profile not owned by the account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
    delete:
      operationId: archiveMyCharacterProfile
      tags: [users]
      summary: Soft-delete an owned profile while preserving historical content
      security: [{ bearerAuth: [] }]
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Remaining profile state
          content: { application/json: { schema: { $ref: "#/components/schemas/CharacterProfileState" } } }
        "409": { description: The final profile cannot be archived }

  /users/me/profiles/{profileId}/inventory:
    get:
      operationId: getMyCharacterProfileInventory
      tags: [users]
      summary: List inventory items owned by one character profile
      security: [{ bearerAuth: [] }]
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Profile inventory
          content:
            application/json:
              schema:
                type: object
                required: [profileId, items]
                properties:
                  profileId: { type: string, format: uuid }
                  items:
                    type: array
                    items: { $ref: "#/components/schemas/CharacterProfileInventoryItem" }
        "403": { description: Profile is not owned by the account }

  /users/me/profile-notifications:
    get:
      operationId: getMyCharacterProfileNotifications
      tags: [users]
      summary: List notifications for the selected character profile
      security: [{ bearerAuth: [] }]
      parameters:
        - in: header
          name: X-Character-Profile-Id
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Profile notification list
          content:
            application/json:
              schema:
                type: object
                required: [profileId, items]
                properties:
                  profileId: { type: string, format: uuid }
                  items:
                    type: array
                    items: { $ref: "#/components/schemas/CharacterProfileNotification" }

  /users/me/profile-notifications/{notificationId}/read:
    patch:
      operationId: markMyCharacterProfileNotificationRead
      tags: [users]
      security: [{ bearerAuth: [] }]
      parameters:
        - in: path
          name: notificationId
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Read notification
          content: { application/json: { schema: { $ref: "#/components/schemas/CharacterProfileNotification" } } }
        "404": { description: Notification not found for selected profile }

  /users/me/profile-social:
    get:
      operationId: getMyCharacterProfileSocial
      tags: [users]
      summary: List followers or following profiles for the selected character profile
      security: [{ bearerAuth: [] }]
      parameters:
        - in: query
          name: scope
          required: false
          schema:
            type: string
            enum: [followers, following]
            default: followers
        - in: header
          name: X-Character-Profile-Id
          required: false
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Character profile social list
          content:
            application/json:
              schema: { $ref: "#/components/schemas/CharacterProfileSocialResponse" }
        "400": { description: Invalid social scope }

  /nft/collections:
    get:
      operationId: listPublishedNftCollections
      tags: [nft]
      summary: List published NFT collections available for STAR summon
      responses:
        "200":
          description: Published collections with public metadata only
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/PublicNftCollection"

  /nft/collections/{id}/evolution:
    get:
      operationId: listPublishedNftEvolutionStages
      tags: [nft]
      summary: List published avatar evolution stages for a collection
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Published evolution stages
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/NftEvolutionStage"
        "404":
          description: Collection is not fully reviewed and published
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /nft/collections/{id}/rpg-content:
    get:
      operationId: getPublishedNftRpgContent
      tags: [nft]
      summary: Get reviewed RPG mission content for a published collection
      parameters:
        - in: path
          name: id
          required: true
          schema: { type: string, format: uuid }
      responses:
        "200":
          description: Collection RPG content
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/NftRpgContent"
        "404":
          description: Collection is not published
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/wallet:
    get:
      operationId: getMyWalletStatus
      tags: [users]
      summary: Get my wallet and NFT verification status
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Wallet verification status
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WalletStatus"

  /users/me/wallet/challenge:
    post:
      operationId: createWalletChallenge
      tags: [users]
      summary: Create a wallet ownership verification challenge
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WalletChallengeInput"
      responses:
        "201":
          description: Signable wallet verification challenge
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WalletChallenge"
        "400":
          description: Invalid wallet address
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/wallet/verify:
    post:
      operationId: verifyWalletChallenge
      tags: [users]
      summary: Verify a wallet signature and refresh NFT ownership
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/WalletVerificationInput"
      responses:
        "200":
          description: Wallet and NFT verification result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WalletVerificationResult"
        "400":
          description: Invalid wallet address or signature
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Challenge not found or expired
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Wallet is claimed by another account
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: NFT ownership check failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/wallet/refresh:
    post:
      operationId: refreshWalletNft
      tags: [users]
      summary: Refresh NFT ownership for my verified wallet
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Refreshed wallet and NFT verification result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WalletVerificationResult"
        "404":
          description: No verified wallet exists
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: NFT ownership check failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/star-profile:
    get:
      operationId: getMyStarProfile
      tags: [users]
      summary: Get my equipped STAR NFT profile
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Equipped STAR profile, if one is equipped
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EquippedStarProfileResponse"

  /users/me/star-profiles:
    get:
      operationId: listMyStarProfiles
      tags: [users]
      summary: List my NFT-backed STAR profiles and the active STAR
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: All STAR profiles owned by the current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarProfilesResponse"

  /users/me/star-profiles/{starProfileId}/activate:
    post:
      operationId: activateMyStarProfile
      tags: [users]
      summary: Make one of my STAR profiles the active STAR identity
      security: [{ bearerAuth: [] }]
      parameters:
        - in: path
          name: starProfileId
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Active STAR profile and current play state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ActivateStarProfileResult"
        "404":
          description: STAR profile is not owned by the current user
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/wallet/nfts:
    get:
      operationId: getMyWalletNftInventory
      tags: [users]
      summary: List eligible NFTs owned by my verified wallet
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Eligible NFT inventory across approved collections
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/WalletNftInventory"
        "409":
          description: Wallet verification is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: NFT ownership check failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/star-nft/equip:
    post:
      operationId: equipStarNft
      tags: [users]
      summary: Equip an owned STAR NFT
      security: [{ bearerAuth: [] }]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/EquipStarNftInput"
      responses:
        "200":
          description: Equipped STAR profile and current play state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/EquipStarNftResult"
        "400":
          description: Invalid NFT token id
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: The verified wallet does not own the token
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: A verified wallet is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "502":
          description: NFT ownership check failed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "503":
          description: NFT contract configuration is unavailable
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/me/torimia:
    get:
      operationId: getTorimiaState
      tags: [users]
      summary: Get my Torimia progression state
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Torimia progression state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TorimiaState"

  /users/me/torimia/open:
    post:
      operationId: openTorimia
      tags: [users]
      summary: Open Torimia and promote my equipped STAR when requirements are met
      security: [{ bearerAuth: [] }]
      responses:
        "200":
          description: Updated Torimia progression state
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TorimiaState"
        "403":
          description: Equipped STAR is not ready to open Torimia
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TorimiaError"
        "409":
          description: An equipped STAR is required
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TorimiaError"
        "500":
          description: Torimia could not be opened
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /users/rankings:
    get:
      operationId: getServiceRankings
      tags: [users]
      summary: Get persona, FAN, STAR, or talk-battle rankings
      security: [{ bearerAuth: [] }]
      parameters:
        - name: scope
          in: query
          required: false
          schema:
            type: string
            enum: [persona, fan, star, battle]
            default: fan
        - name: type
          in: query
          required: false
          schema:
            type: string
            enum: [overall, persuasion, logic, empathy, strategy, archetype, fan_power, support_power, story, charm, stage_presence, bond, lore, wins, win_rate, streak]
        - name: archetype
          in: query
          required: false
          description: Used by persona rankings when type is archetype.
          schema:
            type: string
            enum: [strategist, harmonizer, explorer, pioneer, sage, entertainer, activist, observer]
        - name: limit
          in: query
          required: false
          schema:
            type: integer
            minimum: 1
            maximum: 100
            default: 50
      responses:
        "200":
          description: Service ranking list
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceRanking"

  /battles/{id}/feed-post:
    post:
      operationId: createBattleFeedPost
      tags: [battles]
      summary: Publish my completed battle result to the STAR feed
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/BattleFeedPostInput"
      responses:
        "200":
          description: Existing feed post for this battle result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleFeedPostResult"
        "201":
          description: Created feed post for this battle result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/BattleFeedPostResult"
        "400":
          description: Invalid feed post type
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "403":
          description: Caller is not eligible to publish this battle or as a STAR
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "404":
          description: Battle not found
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"
        "409":
          description: Battle is not completed or a STAR NFT must be equipped
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApiError"

  /calls/{id}/failed:
    post:
      operationId: failCall
      tags: [calls]
      summary: Mark one of my non-terminal calls as failed
      security: [{ bearerAuth: [] }]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        "200":
          description: Failed call, or the existing terminal call
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Call"
        "404":
          description: Call not found or caller is not a participant
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorEnvelope"

  /users/{userId}/posts:
    get:
      tags: [users]
      summary: List a user's public feed posts with cursor pagination
      security: [{ bearerAuth: [] }]
      parameters:
        - name: userId
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: starId
          in: query
          schema: { type: string, format: uuid }
        - name: limit
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 30 }
        - name: cursor
          in: query
          schema: { type: string, format: date-time }
      responses:
        "200":
          description: Public posts page
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ProfilePostsPage" }
  /users/{userId}/growth-records:
    get:
      tags: [users]
      summary: List public growth records with cursor pagination
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: userId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 30 } }
        - { name: cursor, in: query, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: Growth records page
          content:
            application/json:
              schema: { $ref: "#/components/schemas/GrowthRecordsPage" }
  /users/{userId}/battle-results:
    get:
      tags: [users]
      summary: List public battle results with cursor pagination
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: userId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 100, default: 30 } }
        - { name: cursor, in: query, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: Battle results page
          content:
            application/json:
              schema: { $ref: "#/components/schemas/BattleResultsPage" }
  /profiles/{profileId}:
    get:
      operationId: getPublicCharacterProfile
      tags: [users]
      summary: Get one public activity profile and its posts
      security: [{ bearerAuth: [] }]
      parameters:
        - { name: profileId, in: path, required: true, schema: { type: string, format: uuid } }
        - { name: limit, in: query, schema: { type: integer, minimum: 1, maximum: 60, default: 30 } }
        - { name: cursor, in: query, schema: { type: string, format: date-time } }
      responses:
        "200":
          description: Public activity profile
          content:
            application/json:
              schema: { $ref: "#/components/schemas/PublicCharacterProfileResponse" }
        "404":
          description: Profile not found
          content:
            application/json:
              schema: { $ref: "#/components/schemas/ApiError" }

  /profiles/{profileId}/follow:
    post:
      operationId: followCharacterProfile
      tags: [users]
      summary: Follow a public activity profile as the active profile
      security: [{ bearerAuth: [] }]
      parameters: [{ name: profileId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses:
        "200": { description: Followed, content: { application/json: { schema: { $ref: "#/components/schemas/CharacterProfileFollowState" } } } }
        "409": { description: Cannot follow an owned profile, content: { application/json: { schema: { $ref: "#/components/schemas/ApiError" } } } }
    delete:
      operationId: unfollowCharacterProfile
      tags: [users]
      summary: Unfollow a public activity profile as the active profile
      security: [{ bearerAuth: [] }]
      parameters: [{ name: profileId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses:
        "200": { description: Unfollowed, content: { application/json: { schema: { $ref: "#/components/schemas/CharacterProfileFollowState" } } } }

  /users/{userId}/profile:
    get:
      tags: [users]
      summary: Get a public user profile
      security: [{ bearerAuth: [] }]
      parameters: [{ name: userId, in: path, required: true, schema: { type: string, format: uuid } }]
      responses:
        "200": { description: Public profile, content: { application/json: { schema: { $ref: "#/components/schemas/PublicProfile" } } } }
  /star-feed/activities:
    get:
      tags: [star-feed]
      summary: List STAR feed activities
      security: [{ bearerAuth: [] }]
      responses: { "200": { description: Activities, content: { application/json: { schema: { type: array, items: { type: object } } } } } }
  /star-feed/activities/read:
    post:
      tags: [star-feed]
      summary: Mark STAR feed activities as read
      security: [{ bearerAuth: [] }]
      responses: { "204": { description: Marked as read } }
  /fan-communities/{id}/broadcasts:
    get:
      tags: [fan-communities]
      summary: List community broadcasts
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { "200": { description: Broadcasts, content: { application/json: { schema: { type: array, items: { type: object } } } } } }
    post:
      tags: [fan-communities]
      summary: Create a community broadcast
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object } } } }
      responses: { "201": { description: Broadcast created } }
  /fan-communities/{id}/missions:
    get:
      tags: [fan-communities]
      summary: List community missions
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { "200": { description: Missions, content: { application/json: { schema: { type: array, items: { type: object } } } } } }
    post:
      tags: [fan-communities]
      summary: Create a community mission
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      requestBody: { required: true, content: { application/json: { schema: { type: object } } } }
      responses: { "201": { description: Mission created } }
  /fan-missions/{id}/join:
    post:
      tags: [fan-communities]
      summary: Join a fan mission
      security: [{ bearerAuth: [] }]
      parameters: [{ name: id, in: path, required: true, schema: { type: string, format: uuid } }]
      responses: { "200": { description: Mission joined } }

  /admin/profiles/{profileId}/status:
    patch:
      operationId: updateAdminCharacterProfileStatus
      tags: [admin]
      summary: Lock or reactivate a character profile
      security: [{ bearerAuth: [] }]
      parameters:
        - in: path
          name: profileId
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status, reason]
              properties:
                status: { type: string, enum: [active, locked] }
                reason: { type: string, minLength: 2, maxLength: 300 }
      responses:
        "200":
          description: Updated character profile
          content: { application/json: { schema: { $ref: "#/components/schemas/CharacterProfile" } } }
        "400": { description: Invalid profile status request }
        "403": { description: Administrator permission required }
        "404": { description: Character profile not found }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  schemas:
    ApiError:
      type: object
      required: [error]
      properties:
        error:
          type: string
        message:
          type: string

    KnowledgeMemory:
      type: object
      required: [id, userId, memoryType, text, privacyScope, status, confidence, source, createdAt, updatedAt]
      properties:
        id:
          type: string
        userId:
          type: string
        subjectUserId:
          type: ["string", "null"]
        memoryType:
          type: string
        text:
          type: string
        privacyScope:
          type: string
          enum: [official_public, public_profile, user_private, relationship_private, system_internal]
        status:
          type: string
          enum: [draft, approved, rejected, archived, inferred]
        confidence:
          type: integer
        source:
          type: string
        graphMemoryId:
          type: ["string", "null"]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    KnowledgeMemoryCreate:
      type: object
      required: [text]
      properties:
        text:
          type: string
          minLength: 1
        memoryType:
          type: string
          default: preference
        privacyScope:
          type: string
          default: user_private

    KnowledgeMemoryPatch:
      type: object
      properties:
        text:
          type: string
        privacyScope:
          type: string
        status:
          type: string

    AnotherMeToneSyncLevel:
      type: string
      enum: [LOW, MEDIUM, HIGH]

    AnotherMeRelationshipType:
      type: string
      enum: [FRIEND, FAMILY, WORK, PARTNER, UNKNOWN, CUSTOM]

    AnotherMeSettings:
      type: object
      required: [summonEnabled, defaultWaitMinutes, allowFriends, allowFamily, allowWork, allowUnknown, autoReplyEnabled, sensitiveReplyBlocked, toneSyncEnabled, defaultToneSyncLevel]
      properties:
        summonEnabled:
          type: boolean
        defaultWaitMinutes:
          type: integer
        allowFriends:
          type: boolean
        allowFamily:
          type: boolean
        allowWork:
          type: boolean
        allowUnknown:
          type: boolean
        autoReplyEnabled:
          type: boolean
        sensitiveReplyBlocked:
          type: boolean
        toneSyncEnabled:
          type: boolean
        defaultToneSyncLevel:
          $ref: "#/components/schemas/AnotherMeToneSyncLevel"

    AnotherMeSettingsPatch:
      type: object
      properties:
        summonEnabled:
          type: boolean
        defaultWaitMinutes:
          type: integer
          minimum: 1
          maximum: 60
        allowFriends:
          type: boolean
        allowFamily:
          type: boolean
        allowWork:
          type: boolean
        allowUnknown:
          type: boolean
        autoReplyEnabled:
          type: boolean
        sensitiveReplyBlocked:
          type: boolean
        toneSyncEnabled:
          type: boolean
        defaultToneSyncLevel:
          $ref: "#/components/schemas/AnotherMeToneSyncLevel"

    AnotherMeRoomSettings:
      type: object
      required: [roomId, relationshipType, autoReplyLevel]
      properties:
        roomId:
          type: string
        summonEnabled:
          type: ["boolean", "null"]
        waitMinutes:
          type: ["integer", "null"]
        toneSyncLevel:
          oneOf:
            - $ref: "#/components/schemas/AnotherMeToneSyncLevel"
            - type: "null"
        relationshipType:
          $ref: "#/components/schemas/AnotherMeRelationshipType"
        autoReplyLevel:
          type: string

    AnotherMeRoomSettingsPatch:
      type: object
      properties:
        summonEnabled:
          type: ["boolean", "null"]
        waitMinutes:
          type: ["integer", "null"]
          minimum: 1
          maximum: 60
        toneSyncLevel:
          oneOf:
            - $ref: "#/components/schemas/AnotherMeToneSyncLevel"
            - type: "null"
        relationshipType:
          $ref: "#/components/schemas/AnotherMeRelationshipType"
        autoReplyLevel:
          type: string
          minLength: 1
          maxLength: 40

    AnotherMeSummonInput:
      type: object
      required: [roomId, targetUserId]
      properties:
        roomId:
          type: string
          format: uuid
        targetUserId:
          type: string
          format: uuid

    AnotherMeSession:
      type: object
      required: [id, ownerUserId, summonedByUserId, roomId, status, summonedAt, lastActivityAt, expiresAt, isOwner, isCaller, canDismiss]
      properties:
        id:
          type: string
        ownerUserId:
          type: string
        summonedByUserId:
          type: string
        roomId:
          type: string
        status:
          type: string
          enum: [ACTIVE, DISMISSED_BY_OWNER, DISMISSED_BY_CALLER, EXPIRED, FAILED]
        summonedAt:
          type: string
          format: date-time
        dismissedAt:
          type: ["string", "null"]
          format: date-time
        dismissedByUserId:
          type: ["string", "null"]
        lastActivityAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        reason:
          type: ["string", "null"]
        ownerName:
          type: ["string", "null"]
        summonedByName:
          type: ["string", "null"]
        isOwner:
          type: boolean
        isCaller:
          type: boolean
        canDismiss:
          type: boolean

    AnotherMeSummonStatus:
      type: object
      required: [canSummon, waitMinutes, remainingSeconds, targetUserId]
      properties:
        canSummon:
          type: boolean
        reason:
          type: ["string", "null"]
        waitMinutes:
          type: integer
        remainingSeconds:
          type: integer
        targetUserId:
          type: string
        targetUserName:
          type: ["string", "null"]
        activeSession:
          oneOf:
            - $ref: "#/components/schemas/AnotherMeSession"
            - type: "null"

    AnotherMeToneProfileInput:
      type: object
      properties:
        relationshipType:
          allOf:
            - $ref: "#/components/schemas/AnotherMeRelationshipType"
          default: FRIEND

    AnotherMeToneProfile:
      type: object
      required: [id, userId, relationshipType, honorificStyle, averageMessageLength, emojiUsageLevel, laughterUsageLevel, formalityLevel, warmthLevel, humorLevel, commonPhrasesJson, forbiddenPhrasesJson, updatedAt]
      properties:
        id:
          type: string
        userId:
          type: string
        relationshipType:
          $ref: "#/components/schemas/AnotherMeRelationshipType"
        toneSummary:
          type: ["string", "null"]
        honorificStyle:
          type: string
          enum: [BANMAL, JONDAETMAL, MIXED]
        averageMessageLength:
          type: integer
        emojiUsageLevel:
          type: integer
        laughterUsageLevel:
          type: integer
        formalityLevel:
          type: integer
        warmthLevel:
          type: integer
        humorLevel:
          type: integer
        commonPhrasesJson:
          type: array
          items:
            type: string
        forbiddenPhrasesJson:
          type: array
          items:
            type: string
        updatedAt:
          type: string
          format: date-time

    DailyTalkRewardScores:
      type: object
      required: [empathy, communication, trust, positivity, contribution, spamRisk, qualityScore]
      properties:
        empathy:
          type: number
        communication:
          type: number
        trust:
          type: number
        positivity:
          type: number
        contribution:
          type: number
        spamRisk:
          type: number
        qualityScore:
          type: number

    DailyTalkRewardAbuseSignals:
      type: object
      required: [messageCount, userMessageCount, otherMessageCount, counterpartCount, repeatedMessageRatio, shortMessageRatio, selfMessageRatio, rewardMultiplier, reductions]
      properties:
        messageCount:
          type: integer
        userMessageCount:
          type: integer
        otherMessageCount:
          type: integer
        counterpartCount:
          type: integer
        repeatedMessageRatio:
          type: number
        shortMessageRatio:
          type: number
        selfMessageRatio:
          type: number
        rewardMultiplier:
          type: number
        reductions:
          type: array
          items:
            type: string

    DailyTalkRewardStatus:
      type: object
      required: [canClaim, claimedToday, messageCount, minMessageCount, streak]
      properties:
        canClaim:
          type: boolean
        reason:
          type: ["string", "null"]
        claimedToday:
          type: boolean
        messageCount:
          type: integer
        minMessageCount:
          type: integer
        streak:
          type: integer
        rewardId:
          type: ["string", "null"]
        status:
          type: ["string", "null"]

    DailyTalkReward:
      type: object
      required: [id, rewardDate, status, title, mood, keywords, diary, summary, scores, grade, qualityScore, spamRisk, pvtAmount, estimatedPvtAmount, visibility, ontologySyncStatus, createdAt, updatedAt]
      properties:
        id:
          type: string
        rewardDate:
          type: string
          format: date
        status:
          type: string
          enum: [PENDING, GENERATED, SAVED, POSTED, REWARDED, FAILED]
        title:
          type: string
        mood:
          type: string
        keywords:
          type: array
          items:
            type: string
        diary:
          type: string
        summary:
          type: string
        scores:
          $ref: "#/components/schemas/DailyTalkRewardScores"
        grade:
          type: string
        qualityScore:
          type: integer
        spamRisk:
          type: integer
        pvtAmount:
          type: integer
        estimatedPvtAmount:
          type: integer
        visibility:
          type: string
          enum: [PRIVATE, FRIENDS, PUBLIC]
        feedPostId:
          type: ["string", "null"]
        abuse:
          oneOf:
            - $ref: "#/components/schemas/DailyTalkRewardAbuseSignals"
            - type: "null"
        ontologySyncStatus:
          type: string
          enum: [none, pending, processing, processed, retrying, failed]
        ontologySyncedAt:
          type: ["string", "null"]
          format: date-time
        rewardedAt:
          type: ["string", "null"]
          format: date-time
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time

    DailyTalkRewardPatch:
      type: object
      properties:
        title:
          type: string
          minLength: 1
          maxLength: 80
        diary:
          type: string
          minLength: 1
          maxLength: 1200
        visibility:
          type: string
          enum: [PRIVATE, FRIENDS, PUBLIC]

    BibiOfficialProfile:
      type: object
      required: [id, nickname, displayName, handle]
      properties:
        id:
          type: string
        nickname:
          type: string
        displayName:
          type: string
        handle:
          type: string
        profileImageUrl:
          type: ["string", "null"]
        starProfile:
          type: ["object", "null"]
          required: [id, displayName, stage, followedByMe]
          properties:
            id: { type: string, format: uuid }
            displayName: { type: string }
            imageUrl: { type: ["string", "null"] }
            stage: { type: string }
            followedByMe: { type: boolean }
        statusMessage:
          type: ["string", "null"]

    BibiOfficialRoom:
      type: object
      required: [account, room]
      properties:
        account:
          $ref: "#/components/schemas/BibiOfficialProfile"
        room:
          $ref: "#/components/schemas/ChatRoom"

    PresenceHeartbeat:
      type: object
      properties:
        roomId:
          type: ["string", "null"]
          format: uuid
        platform:
          type: ["string", "null"]
          minLength: 1
          maxLength: 32

    PresenceState:
      type: object
      required: [userId, online]
      properties:
        userId:
          type: string
        online:
          type: boolean
        lastSeenAt:
          type: ["string", "null"]
          format: date-time
        platform:
          type: ["string", "null"]
        roomId:
          type: ["string", "null"]

    PresenceUsersResponse:
      type: object
      required: [users]
      properties:
        users:
          type: array
          items:
            $ref: "#/components/schemas/PresenceState"

    PvtWallet:
      type: object
      required: [balance]
      properties:
        balance:
          type: integer
        updatedAt:
          type: ["string", "null"]
          format: date-time

    PvtTransaction:
      type: object
      required: [id, amount, type, source, sourceId, balanceAfter, createdAt]
      properties:
        id:
          type: string
        amount:
          type: integer
        type:
          type: string
          enum: [EARN, SPEND, ADJUST]
        source:
          type: string
          enum: [DAILY_TALK_REWARD, EVENT, ADMIN, MISSION]
        sourceId:
          type: string
        description:
          type: ["string", "null"]
        balanceAfter:
          type: integer
        createdAt:
          type: string
          format: date-time

    StarFeedAuthor:
      type: object
      required: [nickname]
      properties:
        id:
          type: ["string", "null"]
        nickname:
          type: string
        profileImageUrl:
          type: ["string", "null"]

    StarFeedComment:
      type: object
      required: [id, body, createdAt, author]
      properties:
        id:
          type: string
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        author:
          $ref: "#/components/schemas/StarFeedAuthor"

    StarFeedPost:
      type: object
      required: [id, kind, title, body, hashtags, media, visibility, createdAt, author, targetStarProfile, reactionCount, commentCount, reactedByMe, recentComments]
      properties:
        id:
          type: string
        kind:
          type: string
          enum: [official, event, fan, star, growth, profile_update, talk_diary]
        title:
          type: string
        body:
          type: string
        metadata:
          type: ["object", "null"]
          additionalProperties: true
        hashtags:
          type: array
          items: { type: string }
        media:
          type: array
          maxItems: 4
          items:
            $ref: "#/components/schemas/StarFeedMedia"
        visibility:
          type: string
          enum: [PRIVATE, FRIENDS, PUBLIC]
        createdAt:
          type: string
          format: date-time
        author:
          $ref: "#/components/schemas/StarFeedAuthor"
        targetStarProfile:
          oneOf:
            - $ref: "#/components/schemas/StarProfileTarget"
            - type: "null"
        reactionCount:
          type: integer
        commentCount:
          type: integer
        reactedByMe:
          type: boolean
        recentComments:
          type: array
          items:
            $ref: "#/components/schemas/StarFeedComment"

    StarFeedPostInput:
      type: object
      required: [body]
      properties:
        kind:
          type: string
          enum: [fan, star]
          default: fan
        title:
          type: string
          maxLength: 80
        body:
          type: string
          minLength: 1
          maxLength: 500
        targetStarProfileId:
          type: [string, "null"]
          format: uuid
          description: Optional STAR being supported by a FAN post; null means the neutral FAN profile.
        media:
          type: array
          maxItems: 4
          items:
            $ref: "#/components/schemas/StarFeedMedia"

    StarFeedMedia:
      type: object
      required: [objectPath, mediaType]
      properties:
        objectPath: { type: string }
        mediaType: { type: string, enum: [image, video] }
        altText: { type: string, maxLength: 160 }

    StarFeedPage:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/StarFeedPost" }
        nextCursor:
          type: [string, "null"]
          format: date-time

    StarFeedCreateResult:
      type: object
      required: [post, reward]
      properties:
        post: { $ref: "#/components/schemas/StarFeedPost" }
        reward:
          oneOf:
            - $ref: "#/components/schemas/StarFeedReward"
            - type: "null"

    StarFeedReward:
      type: object
      required: [granted, xp, stat]
      properties:
        granted: { type: boolean }
        xp: { type: integer, minimum: 0 }
        stat: { type: string, enum: ["FAN XP"] }

    StarProfileTarget:
      type: object
      required: [id, displayName, imageUrl]
      properties:
        id: { type: string, format: uuid }
        displayName: { type: string }
        imageUrl: { type: [string, "null"] }

    StarProfileFollowState:
      type: object
      required: [following]
      properties:
        following:
          type: boolean

    StarFeedReportInput:
      type: object
      required: [reason]
      properties:
        reason: { type: string, enum: [spam, harassment, sexual, violence, copyright, other] }
        details: { type: string, maxLength: 500 }

    StarFeedReport:
      type: object
      required: [id, postId, reason, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        postId: { type: string, format: uuid }
        reason: { type: string }
        details: { type: [string, "null"] }
        status: { type: string }
        createdAt: { type: string, format: date-time }

    StarResultDraft:
      type: object
      required: [id, title, body, status, createdAt]
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        body: { type: string }
        status: { type: string }
        createdAt: { type: string, format: date-time }

    FanCommunityInput:
      type: object
      required: [starProfileId, name]
      properties:
        starProfileId: { type: string, format: uuid }
        name: { type: string, maxLength: 80 }
        description: { type: string, maxLength: 500 }

    StarFeedCommentInput:
      type: object
      required: [body]
      properties:
        body:
          type: string
          minLength: 1
          maxLength: 240

    ObjectUploadResponse:
      type: object
      required: [objectPath, metadata]
      properties:
        objectPath:
          type: string
        metadata:
          $ref: "#/components/schemas/UploadUrlRequest"

    FanStats:
      type: object
      required: [fanPower, supportPower, empathy, story]
      properties:
        fanPower:
          type: integer
        supportPower:
          type: integer
        empathy:
          type: integer
        story:
          type: integer

    FanProfile:
      type: object
      required: [level, xp, stats]
      properties:
        level:
          type: integer
        xp:
          type: integer
        stats:
          $ref: "#/components/schemas/FanStats"

    StarStats:
      type: object
      required: [charm, stagePresence, bond, lore]
      properties:
        charm:
          type: integer
        stagePresence:
          type: integer
        bond:
          type: integer
        lore:
          type: integer

    StarProfile:
      type: object
      required: [id, starKey, displayName, tokenId, contractAddress, chainId, stage, level, xp, stats]
      properties:
        id:
          type: string
        starKey:
          type: string
        displayName:
          type: string
        tokenId:
          type: string
        contractAddress:
          type: string
        chainId:
          type: integer
        imageUrl:
          type: ["string", "null"]
        stage:
          type: string
          enum: [aspiring, promoted]
        level:
          type: integer
        xp:
          type: integer
        stats:
          $ref: "#/components/schemas/StarStats"
        equippedAt:
          type: ["string", "null"]
          format: date-time
        verifiedAt:
          type: ["string", "null"]
          format: date-time
        torimiaOpenedAt:
          type: ["string", "null"]
          format: date-time
        promotedAt:
          type: ["string", "null"]
          format: date-time

    PlayModeInput:
      type: object
      required: [mode]
      properties:
        mode:
          type: string
          enum: [fan, star]

    PlayModeState:
      type: object
      required: [currentMode, starUnlocked, fanProfile, social, starProfiles]
      properties:
        currentMode:
          type: string
          enum: [fan, star]
        starUnlocked:
          type: boolean
        fanProfile:
          $ref: "#/components/schemas/FanProfile"
        social:
          type: object
          required: [followerCount, followingCount]
          properties:
            followerCount:
              type: integer
              minimum: 0
            followingCount:
              type: integer
              minimum: 0
        equippedStar:
          oneOf:
            - $ref: "#/components/schemas/StarProfile"
            - type: "null"
        starProfiles:
          type: array
          items:
            $ref: "#/components/schemas/StarProfile"

    CharacterProfileSummary:
      type: object
      required: [id, type, handle, displayName]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          enum: [fan, star, official_ai]
        handle:
          type: string
        displayName:
          type: string
        profileImageUrl:
          type: ["string", "null"]
        activityProfile:
          oneOf:
            - $ref: "#/components/schemas/CharacterProfileSummary"
            - type: "null"

    CharacterProfile:
      allOf:
        - $ref: "#/components/schemas/CharacterProfileSummary"
        - type: object
          required: [status, level, xp, jobStage, stats, metadata, isActive]
          properties:
            statusMessage:
              type: ["string", "null"]
            status:
              type: string
              enum: [active, locked, torimia, archived]
            level:
              type: integer
              minimum: 1
            xp:
              type: integer
              minimum: 0
            jobKey:
              type: [string, "null"]
            jobStage:
              type: integer
              minimum: 0
            stats:
              type: object
              additionalProperties:
                type: number
            metadata:
              type: object
              additionalProperties: true
            isActive:
              type: boolean

    CharacterProfileInventoryItem:
      type: object
      required: [id, profileId, itemKey, itemType, quantity, equipped, metadata, updatedAt]
      properties:
        id: { type: string, format: uuid }
        profileId: { type: string, format: uuid }
        itemKey: { type: string }
        itemType: { type: string }
        quantity: { type: integer, minimum: 0 }
        equipped: { type: boolean }
        metadata: { type: object, additionalProperties: true }
        updatedAt: { type: string, format: date-time }

    CharacterProfileNotification:
      type: object
      required: [id, profileId, type, data, createdAt]
      properties:
        id: { type: string, format: uuid }
        profileId: { type: string, format: uuid }
        actorProfileId: { type: [string, "null"], format: uuid }
        type: { type: string }
        data: { type: object, additionalProperties: true }
        readAt: { type: [string, "null"], format: date-time }
        createdAt: { type: string, format: date-time }

    CharacterProfileSocialEntry:
      allOf:
        - $ref: "#/components/schemas/CharacterProfileSummary"
        - type: object
          required: [statusMessage, followedAt]
          properties:
            statusMessage: { type: ["string", "null"] }
            followedAt: { type: string, format: date-time }

    CharacterProfileSocialResponse:
      type: object
      required: [profileId, scope, profiles]
      properties:
        profileId: { type: string, format: uuid }
        scope: { type: string, enum: [followers, following] }
        profiles:
          type: array
          items: { $ref: "#/components/schemas/CharacterProfileSocialEntry" }

    CharacterProfileState:
      type: object
      required: [activeProfile, profiles]
      properties:
        activeProfile:
          $ref: "#/components/schemas/CharacterProfile"
        profiles:
          type: array
          items:
            $ref: "#/components/schemas/CharacterProfile"

    StarProfilesResponse:
      type: object
      required: [starProfiles]
      properties:
        starProfiles:
          type: array
          items:
            $ref: "#/components/schemas/StarProfile"

    ActivateStarProfileResult:
      type: object
      required: [equippedStar, state]
      properties:
        equippedStar:
          $ref: "#/components/schemas/StarProfile"
        state:
          $ref: "#/components/schemas/PlayModeState"

    PlayModeError:
      allOf:
        - $ref: "#/components/schemas/ApiError"
        - type: object
          required: [state]
          properties:
            state:
              $ref: "#/components/schemas/PlayModeState"

    WalletStatus:
      type: object
      required: [walletVerified, nftVerified, nftConfigured, starUnlocked]
      properties:
        walletAddress:
          type: ["string", "null"]
        chainId:
          type: ["integer", "null"]
        walletVerified:
          type: boolean
        nftVerified:
          type: boolean
        lastCheckedAt:
          type: ["string", "null"]
          format: date-time
        nftContractAddress:
          type: ["string", "null"]
        nftChainId:
          type: ["integer", "null"]
        nftConfigured:
          type: boolean
        starUnlocked:
          type: boolean

    WalletChallengeInput:
      type: object
      required: [walletAddress]
      properties:
        walletAddress:
          type: string
          minLength: 1
          maxLength: 120
        chainId:
          type: integer
          minimum: 1
          maximum: 2147483647

    WalletChallenge:
      type: object
      required: [id, walletAddress, message, expiresAt, chainId, domain, uri]
      properties:
        id:
          type: string
        walletAddress:
          type: string
        message:
          type: string
        expiresAt:
          type: string
          format: date-time
        nftContractAddress:
          type: ["string", "null"]
        nftChainId:
          type: ["integer", "null"]
        chainId:
          type: integer
        domain:
          type: string
        uri:
          type: string
          format: uri

    WalletVerificationInput:
      type: object
      required: [walletAddress, challengeId, signature]
      properties:
        walletAddress:
          type: string
          minLength: 1
          maxLength: 120
        challengeId:
          type: string
          format: uuid
        signature:
          type: string
          minLength: 1
          maxLength: 500

    WalletVerificationResult:
      type: object
      required: [ok, status, nftOwned, configMissing]
      properties:
        ok:
          type: boolean
          const: true
        status:
          $ref: "#/components/schemas/WalletStatus"
        nftOwned:
          type: boolean
        balance:
          type: ["string", "null"]
        configMissing:
          type: boolean

    WalletNftToken:
      type: object
      required: [collectionId, chainId, contractAddress, collectionName, ipName, category, tokenId]
      properties:
        collectionId: { type: string, format: uuid }
        chainId: { type: integer }
        contractAddress: { type: string }
        collectionName: { type: string }
        ipName: { type: string }
        category: { type: string }
        tokenId: { type: string }

    WalletNftCollection:
      type: object
      required: [collectionId, chainId, contractAddress, collectionName, ipName, category, balance, enumerable, truncated, requiresTokenId, tokens]
      properties:
        collectionId: { type: string, format: uuid }
        chainId: { type: integer }
        contractAddress: { type: string }
        collectionName: { type: string }
        ipName: { type: string }
        category: { type: string }
        balance: { type: string }
        enumerable: { type: boolean }
        truncated: { type: boolean }
        requiresTokenId: { type: boolean }
        tokens:
          type: array
          items:
            $ref: "#/components/schemas/WalletNftToken"

    WalletNftInventory:
      type: object
      required: [walletAddress, collections, totalOwned, hasEligibleNft, configuredCollectionCount, failedCollectionCount, partial, checkedAt]
      properties:
        walletAddress: { type: string }
        collections:
          type: array
          items:
            $ref: "#/components/schemas/WalletNftCollection"
        totalOwned: { type: integer, minimum: 0 }
        hasEligibleNft: { type: boolean }
        configuredCollectionCount: { type: integer, minimum: 0 }
        failedCollectionCount: { type: integer, minimum: 0 }
        partial: { type: boolean }
        checkedAt: { type: string, format: date-time }

    PublicNftCollection:
      type: object
      required: [id, chainId, contractAddress, name, ipName, category, status, updatedAt]
      properties:
        id: { type: string, format: uuid }
        chainId: { type: integer, minimum: 1 }
        contractAddress: { type: string }
        name: { type: string }
        ipName: { type: string }
        category: { type: string }
        officialUrl: { type: ["string", "null"], format: uri }
        roleName: { type: ["string", "null"] }
        worldStyle: { type: ["string", "null"] }
        status:
          type: string
          const: published
        updatedAt: { type: string, format: date-time }

    NftEvolutionStage:
      type: object
      required: [id, collectionId, stageKey, minLevel, title, description, status, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        collectionId: { type: string, format: uuid }
        stageKey: { type: string }
        minLevel: { type: integer, minimum: 1 }
        title: { type: string }
        description: { type: string }
        retainedTraits:
          type: array
          items: { type: string }
        imageUrl: { type: ["string", "null"] }
        status:
          type: string
          const: published
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }

    NftRpgMission:
      type: object
      required: [id, title, description, xp]
      properties:
        id: { type: string }
        title: { type: string }
        description: { type: string }
        xp: { type: integer, minimum: 1 }

    NftRpgContent:
      type: object
      required: [collectionId, ipName, category, roleName, worldStyle, missions, story]
      properties:
        collectionId: { type: string, format: uuid }
        ipName: { type: string }
        category: { type: string }
        roleName: { type: string }
        worldStyle: { type: string }
        missions:
          type: array
          items:
            $ref: "#/components/schemas/NftRpgMission"
        story:
          type: object
          required: [opening, next]
          properties:
            opening: { type: string }
            next: { type: string }

    EquippedStarProfileResponse:
      type: object
      required: [equippedStar]
      properties:
        equippedStar:
          oneOf:
            - $ref: "#/components/schemas/StarProfile"
            - type: "null"

    EquipStarNftInput:
      type: object
      required: [tokenId]
      properties:
        tokenId:
          type: string
          minLength: 1
          maxLength: 80
        collectionId:
          type: string
          format: uuid

    EquipStarNftResult:
      type: object
      required: [equippedStar, state]
      properties:
        equippedStar:
          $ref: "#/components/schemas/StarProfile"
        state:
          $ref: "#/components/schemas/PlayModeState"

    TorimiaRequirement:
      type: object
      required: [key, label, current, target, met]
      properties:
        key:
          type: string
          enum: [level, missions, charm, stagePresence, bond, lore]
        label:
          type: string
        current:
          type: integer
        target:
          type: integer
        met:
          type: boolean

    TorimiaState:
      type: object
      required: [opened, promoted, canOpen, requirements]
      properties:
        star:
          oneOf:
            - $ref: "#/components/schemas/StarProfile"
            - type: "null"
        opened:
          type: boolean
        promoted:
          type: boolean
        canOpen:
          type: boolean
        requirements:
          type: array
          items:
            $ref: "#/components/schemas/TorimiaRequirement"

    TorimiaError:
      allOf:
        - $ref: "#/components/schemas/ApiError"
        - type: object
          required: [state]
          properties:
            state:
              $ref: "#/components/schemas/TorimiaState"

    ServiceRankingItem:
      type: object
      required: [id, rank, userId, displayName, level, title, subTitle, badgeLabel, score, primaryStatLabel, primaryStatValue]
      properties:
        id:
          type: string
        rank:
          type: integer
        userId:
          type: string
        displayName:
          type: string
        avatarUrl:
          type: ["string", "null"]
        level:
          type: integer
        title:
          type: string
        subTitle:
          type: string
        badgeLabel:
          type: string
        score:
          type: integer
        primaryStatLabel:
          type: string
        primaryStatValue:
          type: integer

    ServiceRanking:
      type: object
      required: [scope, type, items]
      properties:
        scope:
          type: string
          enum: [persona, fan, star, battle]
        type:
          type: string
          enum: [overall, persuasion, logic, empathy, strategy, archetype, fan_power, support_power, story, charm, stage_presence, bond, lore, wins, win_rate, streak]
        archetype:
          type: ["string", "null"]
        items:
          type: array
          items:
            $ref: "#/components/schemas/ServiceRankingItem"
        myRank:
          oneOf:
            - $ref: "#/components/schemas/PersonaRankingMyRank"
            - type: "null"

    BattleFeedPostInput:
      type: object
      properties:
        kind:
          type: string
          enum: [fan, star]
          default: fan

    BattleVibeSummary:
      type: object
      required: [key, label, description, topAxes]
      properties:
        key:
          type: string
        label:
          type: string
        description:
          type: string
        topAxes:
          type: array
          items:
            type: string
            enum: [logic, persuasiveness, rebuttal, wit, manners]

    BattleResultReward:
      type: object
      required: [label, value]
      properties:
        label:
          type: string
        value:
          type: string

    BattleResultSummary:
      type: object
      required: [source, battleRoomId, matchSeq, topic, opponentName, outcome, outcomeLabel, myScore, opponentScore, vibe, rewards]
      properties:
        source:
          type: string
          const: battle
        battleRoomId:
          type: string
        matchSeq:
          type: integer
        topic:
          type: string
        opponentName:
          type: string
        outcome:
          type: string
          enum: [win, loss, draw]
        outcomeLabel:
          type: string
        myScore:
          type: integer
        opponentScore:
          type: integer
        vibe:
          $ref: "#/components/schemas/BattleVibeSummary"
        rewards:
          type: array
          items:
            $ref: "#/components/schemas/BattleResultReward"

    BattleFeedPostResult:
      type: object
      required: [post, duplicate, summary]
      properties:
        post:
          $ref: "#/components/schemas/StarFeedPost"
        duplicate:
          type: boolean
        summary:
          $ref: "#/components/schemas/BattleResultSummary"

    Quest:
      type: object
      required:
        [key, type, title, description, progress, target, completed, rewardClaimed, rewardExp]
      properties:
        key: { type: string }
        type: { type: string, enum: [daily, weekly] }
        title: { type: string }
        description: { type: string }
        progress: { type: integer }
        target: { type: integer }
        completed: { type: boolean }
        rewardClaimed: { type: boolean }
        rewardExp: { type: integer }
    Achievement:
      type: object
      required:
        [key, title, description, category, icon, unlocked, rewardClaimed, rewardExp]
      properties:
        key: { type: string }
        title: { type: string }
        description: { type: string }
        category:
          type: string
          enum: [chat, battle, dungeon, clan, persona]
        icon: { type: string }
        unlocked: { type: boolean }
        rewardClaimed: { type: boolean }
        rewardExp: { type: integer }
    RewardClaimResult:
      type: object
      required: [ok, rewardExp]
      properties:
        ok: { type: boolean }
        rewardExp: { type: integer }
    RewardClaimError:
      type: object
      required: [error]
      properties:
        error: { type: string }
        message: { type: string }
    RewardsSummary:
      type: object
      required: [claimableQuests, claimableAchievements, total]
      properties:
        claimableQuests: { type: integer }
        claimableAchievements: { type: integer }
        total: { type: integer }
    UploadUrlRequest:
      type: object
      required: [name, size, contentType]
      properties:
        name:
          type: string
          minLength: 1
        size:
          type: integer
          minimum: 1
        contentType:
          type: string
          minLength: 1
    UploadUrlResponse:
      type: object
      required: [uploadURL, objectPath]
      properties:
        uploadURL:
          type: string
          format: uri
        objectPath:
          type: string
        metadata:
          $ref: "#/components/schemas/UploadUrlRequest"
    MediaUrlRequest:
      type: object
      required: [objectPath]
      properties:
        objectPath:
          type: string
          minLength: 10
          maxLength: 1024
          pattern: ^/objects/
    MediaUrlResponse:
      type: object
      required: [url]
      properties:
        url:
          type: string
    ErrorEnvelope:
      type: object
      required: [error]
      properties:
        error:
          type: string
    HealthStatus:
      type: object
      required: [status]
      properties:
        status:
          type: string

    RealtimeTicket:
      type: object
      required: [ticket]
      properties:
        ticket:
          type: string

    UserProfile:
      type: object
      required: [id, clerkId, email, nickname, notificationEnabled, createdAt]
      properties:
        id:
          type: string
        clerkId:
          type: string
        email:
          type: string
        nickname:
          type: string
        profileImageUrl:
          type: ["string", "null"]
          description: Deprecated legacy field. Always null; use the active character profile.
        statusMessage:
          type: ["string", "null"]
        pushToken:
          type: ["string", "null"]
        notificationEnabled:
          type: boolean
        createdAt:
          type: string

    PublicCharacterIdentity:
      type: object
      required: [id, type, handle, displayName, profileImageUrl]
      properties:
        id:
          type: string
          format: uuid
        type:
          type: string
          enum: [fan, star, official_ai]
        handle:
          type: string
        displayName:
          type: string
        profileImageUrl:
          type: ["string", "null"]
        statusMessage:
          type: ["string", "null"]

    PersonaStats:
      type: object
      required: [logic, empathy, wit, knowledge, conviction, emotion, decisiveness]
      properties:
        logic:
          type: integer
        empathy:
          type: integer
        wit:
          type: integer
        knowledge:
          type: integer
        conviction:
          type: integer
        emotion:
          type: integer
        decisiveness:
          type: integer

    GrowthEvent:
      type: object
      required:
        [id, sourceType, eventType, expDelta, beforeLevel, afterLevel, beforeExp, afterExp, createdAt]
      properties:
        id:
          type: string
        sourceType:
          type: string
        eventType:
          type: string
        sourceId:
          type: ["string", "null"]
        expDelta:
          type: integer
        statChanges:
          type: object
          additionalProperties:
            type: integer
        reason:
          type: ["string", "null"]
        beforeLevel:
          type: integer
        afterLevel:
          type: integer
        beforeExp:
          type: integer
        afterExp:
          type: integer
        createdAt:
          type: string

    PersonaProfile:
      type: object
      required:
        [id, userId, level, xp, xpIntoLevel, xpForNextLevel, stats, recentEvents, createdAt]
      properties:
        id:
          type: string
        userId:
          type: string
        level:
          type: integer
        xp:
          type: integer
        xpIntoLevel:
          type: integer
        xpForNextLevel:
          type: integer
        stats:
          $ref: "#/components/schemas/PersonaStats"
        recentEvents:
          type: array
          items:
            $ref: "#/components/schemas/GrowthEvent"
        lastAnalyzedAt:
          type: ["string", "null"]
        createdAt:
          type: string

    PersonaIdentityHistoryItem:
      type: object
      required: [archetype, level, createdAt]
      properties:
        archetype:
          type: string
        level:
          type: integer
        createdAt:
          type: string

    PersonaCard:
      type: object
      required:
        [
          source,
          syncState,
          name,
          level,
          displayLevelLabel,
          title,
          archetype,
          archetypeKey,
          strengths,
          weaknesses,
          primaryTraits,
          growthDirection,
          motto,
          nextActions,
          syncTimeline,
        ]
      properties:
        source:
          type: string
          enum: [ontology]
        syncState:
          type: string
          enum: [not_started, forming, synced]
        name:
          type: string
        level:
          type: integer
        displayLevelLabel:
          type: ["string", "null"]
        title:
          type: string
        archetype:
          type: string
        archetypeKey:
          type: string
        strengths:
          type: array
          items:
            type: string
        weaknesses:
          type: array
          items:
            type: string
        primaryTraits:
          type: array
          items:
            type: string
        growthDirection:
          type: string
        motto:
          type: string
        houseName:
          type: ["string", "null"]
        nextActions:
          type: array
          items:
            type: string
        syncTimeline:
          type: array
          items:
            type: object
            required: [label, createdAt]
            properties:
              label:
                type: string
              createdAt:
                type: ["string", "null"]
    PersonaRankingItem:
      type: object
      required:
        [
          rank,
          userId,
          displayName,
          level,
          title,
          archetype,
          archetypeLabel,
          score,
          primaryStatLabel,
          primaryStatValue,
        ]
      properties:
        rank:
          type: integer
        userId:
          type: string
        displayName:
          type: string
        avatarUrl:
          type: ["string", "null"]
        level:
          type: integer
        title:
          type: string
        archetype:
          type: string
        archetypeLabel:
          type: string
        score:
          type: integer
        primaryStatLabel:
          type: string
        primaryStatValue:
          type: integer

    PersonaRankingMyRank:
      type: object
      required: [rank, score, pointsToNextRank]
      properties:
        rank:
          type: integer
        score:
          type: integer
        pointsToNextRank:
          type: integer

    PersonaRanking:
      type: object
      required: [type, items]
      properties:
        type:
          type: string
        archetype:
          type: ["string", "null"]
        items:
          type: array
          items:
            $ref: "#/components/schemas/PersonaRankingItem"
        myRank:
          oneOf:
            - $ref: "#/components/schemas/PersonaRankingMyRank"
            - type: "null"

    PersonaAnalysisError:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
        message:
          type: string
        retryAfterSec:
          type: integer

    UserProfileUpdate:
      type: object
      properties:
        nickname:
          type: string
          minLength: 1
          maxLength: 30
        statusMessage:
          type: ["string", "null"]
          maxLength: 200
        notificationEnabled:
          type: boolean
        talkAnalysisEnabled:
          type: boolean

    ProfileUpdateHistoryItem:
      type: object
      required: [id, userId, kind, isVisible, createdAt]
      properties:
        id:
          type: string
        userId:
          type: string
        kind:
          type: string
          enum: [profile_image, status_message, profile_update]
        oldProfileImageUrl:
          type: ["string", "null"]
        newProfileImageUrl:
          type: ["string", "null"]
        oldStatusMessage:
          type: ["string", "null"]
        newStatusMessage:
          type: ["string", "null"]
        feedPostId:
          type: ["string", "null"]
        isVisible:
          type: boolean
        createdAt:
          type: string

    PushTokenInput:
      type: object
      required: [token]
      properties:
        token:
          type: string
          minLength: 1
          maxLength: 8192

    GlobalSearchResponse:
      type: object
      required: [query, type, users, starProfiles, posts, missions, nextCursor]
      properties:
        query: { type: string }
        type: { type: string, enum: [all, users, stars, fans, posts, missions] }
        users:
          type: array
          items:
            type: object
            required: [id, nickname, handle, profileImageUrl, statusMessage, profileType, isMe, followedByMe]
            properties:
              id: { type: string, format: uuid }
              nickname: { type: string }
              handle: { type: string }
              profileImageUrl: { type: [string, "null"] }
              statusMessage: { type: [string, "null"] }
              profileType: { type: string, enum: [fan, star, official_ai] }
              isMe: { type: boolean }
              followedByMe: { type: boolean }
        starProfiles:
          type: array
          items:
            type: object
            required: [id, displayName, starKey, imageUrl, stage, ownerId, followedByMe]
            properties:
              id: { type: string, format: uuid }
              displayName: { type: string }
              starKey: { type: string }
              imageUrl: { type: [string, "null"] }
              stage: { type: string }
              ownerId: { type: [string, "null"], format: uuid }
              followedByMe: { type: boolean }
        posts:
          type: array
          items:
            type: object
            required: [id, title, body, kind, createdAt]
            properties:
              id: { type: string, format: uuid }
              title: { type: string }
              body: { type: string }
              kind: { type: string }
              createdAt: { type: string, format: date-time }
        missions:
          type: array
          items:
            type: object
            required: [key, type, title, description, target, rewardExp, metric]
            properties:
              key: { type: string }
              type: { type: string, enum: [daily, weekly] }
              title: { type: string }
              description: { type: string }
              target: { type: integer }
              rewardExp: { type: integer }
              metric: { type: string }
        nextCursor: { type: [string, "null"] }

    SearchRecommendation:
      type: object
      required: [id, type, handle, displayName, profileImageUrl, statusMessage, level, followedByMe, recommendationReason]
      properties:
        id: { type: string, format: uuid }
        type: { type: string, enum: [fan, star, official_ai] }
        handle: { type: string }
        displayName: { type: string }
        profileImageUrl: { type: [string, "null"] }
        statusMessage: { type: [string, "null"] }
        level: { type: integer }
        followedByMe: { type: boolean }
        recommendationReason: { type: string }

    PublicUser:
      type: object
      required: [id, nickname, accountKind]
      properties:
        id:
          type: string
        nickname:
          type: string
        accountKind:
          type: string
          enum: [user, official, system]
        friendAlias:
          type: ["string", "null"]
          description: Current viewer's saved name for this user, when available.
        displayName:
          type: string
          description: friendAlias when set, otherwise nickname.
        profileImageUrl:
          type: ["string", "null"]
          description: Active character avatar. The legacy member account photo is never exposed.
        statusMessage:
          type: ["string", "null"]
        profile:
          oneOf:
            - $ref: "#/components/schemas/PublicCharacterIdentity"
            - type: "null"

    FriendAliasInput:
      type: object
      required: [alias]
      properties:
        alias:
          type: ["string", "null"]
          maxLength: 30
          description: Null or an empty string clears the saved name.

    FriendRequestInput:
      type: object
      required: [toUserId]
      properties:
        toUserId:
          type: string
          format: uuid

    FriendRequest:
      type: object
      required: [id, fromUserId, toUserId, status, createdAt]
      properties:
        id:
          type: string
        fromUserId:
          type: string
        toUserId:
          type: string
        status:
          type: string
        createdAt:
          type: string

    FriendRequestWithUser:
      type: object
      required: [id, fromUserId, toUserId, status, createdAt, user]
      properties:
        id:
          type: string
        fromUserId:
          type: string
        toUserId:
          type: string
        status:
          type: string
        createdAt:
          type: string
        user:
          $ref: "#/components/schemas/PublicUser"

    ChatRoom:
      type: object
      required: [id, type, category, visibility, createdAt]
      properties:
        id:
          type: string
        type:
          type: string
        category:
          type: string
          enum: [direct, fanclub, counseling, friend_finding, meetup, casual, peer, karaoke, growth_rpg, talk_battle]
        visibility:
          type: string
          enum: [private, invite_only]
        name:
          type: ["string", "null"]
        ownerId:
          type: ["string", "null"]
        lastMessage:
          type: ["string", "null"]
        lastMessageAt:
          type: ["string", "null"]
        lastMessageSeq:
          type: integer
        pinnedMessageId:
          type: ["string", "null"]
        pinnedMessage:
          anyOf:
            - $ref: "#/components/schemas/MessageSummary"
            - type: "null"
        unreadCount:
          type: integer
        firstUnreadMessageId:
          type: ["string", "null"]
        muted:
          type: boolean
        createdAt:
          type: string
        members:
          type: array
          items:
            $ref: "#/components/schemas/PublicUser"

    MessageSummary:
      type: object
      required: [id, senderId, type, content, createdAt]
      properties:
        id:
          type: string
        senderId:
          type: string
        senderName:
          type: ["string", "null"]
        type:
          type: string
        content:
          type: string
        createdAt:
          type: string
        deletedAt:
          type: ["string", "null"]

    ChatRoomMember:
      type: object
      required: [id, roomId, userId, joinedAt, muted]
      properties:
        id:
          type: string
        roomId:
          type: string
        userId:
          type: string
        joinedAt:
          type: string
        muted:
          type: boolean

    RoomInput:
      type: object
      required: [type, memberIds]
      properties:
        type:
          type: string
          enum: [direct, group]
        name:
          type: ["string", "null"]
          maxLength: 30
        category:
          type: string
          enum: [direct, fanclub, counseling, friend_finding, meetup, casual, peer, karaoke, growth_rpg, talk_battle]
        visibility:
          type: string
          enum: [private, invite_only]
        memberIds:
          type: array
          maxItems: 100
          items:
            type: string
            format: uuid

    InviteMembersInput:
      type: object
      required: [memberIds]
      properties:
        memberIds:
          type: array
          minItems: 1
          maxItems: 50
          items:
            type: string
            format: uuid

    Message:
      type: object
      required: [id, roomId, roomSeq, senderId, authorKind, type, content, createdAt]
      properties:
        id:
          type: string
        roomId:
          type: string
        roomSeq:
          type: integer
        senderId:
          type: string
        senderProfile:
          oneOf:
            - $ref: "#/components/schemas/CharacterProfileSummary"
            - type: "null"
        authorKind:
          type: string
        type:
          type: string
        content:
          type: string
        replyToMessageId:
          type: ["string", "null"]
        anotherMeSessionId:
          type: ["string", "null"]
        callId:
          type: ["string", "null"]
        metadata:
          type: ["object", "null"]
          additionalProperties: {}
        clientMessageId:
          type: ["string", "null"]
        deletedAt:
          type: ["string", "null"]
        createdAt:
          type: string
        sender:
          $ref: "#/components/schemas/PublicUser"
        replyTo:
          anyOf:
            - $ref: "#/components/schemas/MessageReplyPreview"
            - type: "null"
        stickerBadges:
          type: array
          items:
            $ref: "#/components/schemas/MessageStickerBadge"
        linkPreview:
          anyOf:
            - $ref: "#/components/schemas/MessageLinkPreview"
            - type: "null"
        readCount:
          type: integer
          description: Number of other room members who have read this message

    MessageReplyPreview:
      type: object
      required: [id, senderId, type, content]
      properties:
        id:
          type: string
        senderId:
          type: string
        senderName:
          type: ["string", "null"]
        type:
          type: string
        content:
          type: string
        deletedAt:
          type: ["string", "null"]

    MessageStickerBadge:
      type: object
      required: [id, code, userId, createdAt]
      properties:
        id:
          type: string
        code:
          type: string
        userId:
          type: string
        createdAt:
          type: string
        user:
          anyOf:
            - $ref: "#/components/schemas/PublicUser"
            - type: "null"

    MessageLinkPreview:
      type: object
      required: [url]
      properties:
        url:
          type: string
        domain:
          type: ["string", "null"]
        title:
          type: ["string", "null"]
        description:
          type: ["string", "null"]
        imageUrl:
          type: ["string", "null"]

    MessageInput:
      type: object
      required: [content]
      properties:
        content:
          type: string
          maxLength: 4096
        type:
          type: string
          enum: [text, image, file, sticker]
        replyToMessageId:
          type: ["string", "null"]
          format: uuid
        metadata:
          type: ["object", "null"]
          maxProperties: 20
          additionalProperties: {}
        clientMessageId:
          type: ["string", "null"]
          minLength: 1
          maxLength: 128

    DeleteMessageInput:
      type: object
      required: [scope]
      properties:
        scope:
          type: string
          enum: [me, everyone]

    PinMessageInput:
      type: object
      required: [messageId]
      properties:
        messageId:
          type: string

    ForwardMessageInput:
      type: object
      required: [targetRoomId]
      properties:
        targetRoomId:
          type: string
        clientMessageId:
          type: ["string", "null"]
          minLength: 1
          maxLength: 128

    StickerBadgeInput:
      type: object
      required: [code]
      properties:
        code:
          type: string

    DungeonInput:
      type: object
      required: [memberIds]
      properties:
        name:
          type: ["string", "null"]
          minLength: 1
          maxLength: 120
        memberIds:
          type: array
          maxItems: 20
          items:
            type: string
            format: uuid
        theme:
          type: ["string", "null"]
          minLength: 1
          maxLength: 200

    DungeonCharacter:
      type: object
      required: [userId, name, hp, maxHp, status, inventory]
      properties:
        userId:
          type: string
        name:
          type: string
        hp:
          type: integer
        maxHp:
          type: integer
        status:
          type: string
        inventory:
          type: array
          items:
            type: string

    DungeonEnemy:
      type: object
      required: [name, hp, maxHp]
      properties:
        name:
          type: string
        hp:
          type: integer
        maxHp:
          type: integer

    DungeonEvent:
      type: object
      required: [kind, text]
      properties:
        kind:
          type: string
        text:
          type: string
        targetUserId:
          type: ["string", "null"]
        points:
          type: integer
          description: For score events, the signed game-point delta (+gain / -loss).

    DungeonGoal:
      type: object
      required: [text, kind, done]
      properties:
        text:
          type: string
        kind:
          type: string
          description: "main (win condition) or sub (intermediate step)"
        done:
          type: boolean

    DungeonState:
      type: object
      required:
        [roomId, theme, status, scene, turn, ended, party, enemies, choices, lastTurnEvents, goals, points]
      properties:
        roomId:
          type: string
        theme:
          type: string
        status:
          type: string
        scene:
          type: string
        turn:
          type: integer
        ended:
          type: boolean
        points:
          type: integer
          description: Shared party score; good choices add, bad choices subtract.
        goals:
          type: array
          items:
            $ref: "#/components/schemas/DungeonGoal"
        party:
          type: array
          items:
            $ref: "#/components/schemas/DungeonCharacter"
        enemies:
          type: array
          items:
            $ref: "#/components/schemas/DungeonEnemy"
        choices:
          type: array
          items:
            type: string
        lastTurnEvents:
          type: array
          items:
            $ref: "#/components/schemas/DungeonEvent"
        lastNarrativeMessageId:
          type: string
          description: Message id of the current turn's DM narrative bubble; the client shows choices only once this message is on screen.

    PersonaStatChanges:
      type: object
      description: Signed deltas applied to a persona's seven growth stats. All optional.
      properties:
        logic:
          type: integer
        empathy:
          type: integer
        wit:
          type: integer
        knowledge:
          type: integer
        conviction:
          type: integer
        emotion:
          type: integer
        decisiveness:
          type: integer

    LifeQuestChoice:
      type: object
      required: [id, label, description, resultText, statChanges, riskLevel]
      properties:
        id:
          type: string
        label:
          type: string
        description:
          type: string
        resultText:
          type: string
        statChanges:
          $ref: "#/components/schemas/PersonaStatChanges"
        riskLevel:
          type: string
          description: "low | medium | high (boldness, not correctness)"

    LifeQuestStage:
      type: object
      required: [stageNumber, title, situation, choices]
      properties:
        stageNumber:
          type: integer
        title:
          type: string
        situation:
          type: string
        choices:
          type: array
          items:
            $ref: "#/components/schemas/LifeQuestChoice"
        chosenChoiceId:
          type: ["string", "null"]

    LifeQuest:
      type: object
      required: [id, userId, title, theme, goal, summary, currentStageIndex, status, stages, createdAt]
      properties:
        id:
          type: string
        userId:
          type: string
        title:
          type: string
        theme:
          type: string
        goal:
          type: string
        summary:
          type: string
        currentStageIndex:
          type: integer
        status:
          type: string
          description: "active | completed | failed"
        stages:
          type: array
          items:
            $ref: "#/components/schemas/LifeQuestStage"
        createdAt:
          type: string
        completedAt:
          type: ["string", "null"]

    LifeQuestActiveResponse:
      type: object
      required: [quest]
      properties:
        quest:
          anyOf:
            - $ref: "#/components/schemas/LifeQuest"
            - type: "null"

    LifeQuestCreateInput:
      type: object
      properties:
        theme:
          type: ["string", "null"]
          description: One of the Life Quest themes, or omit/null for a random theme.

    LifeQuestChooseInput:
      type: object
      required: [stageNumber, choiceId]
      properties:
        stageNumber:
          type: integer
        choiceId:
          type: string

    LifeQuestChooseResult:
      type: object
      required: [quest, resultText, statChanges, expEarned, completed]
      properties:
        quest:
          $ref: "#/components/schemas/LifeQuest"
        resultText:
          type: string
        statChanges:
          $ref: "#/components/schemas/PersonaStatChanges"
        expEarned:
          type: integer
        completed:
          type: boolean

    BattleCreateInput:
      type: object
      required: [category, topic]
      properties:
        memberId:
          type: string
          format: uuid
          description: The single friend's userId to invite. Provide this OR aiPersonaId.
        aiPersonaId:
          type: string
          minLength: 1
          maxLength: 100
          description: The AI opponent persona id to battle against. Provide this OR memberId.
        category:
          type: string
          maxLength: 80
        topic:
          type: string
          minLength: 1
          maxLength: 300

    BattleTopicInput:
      type: object
      required: [category]
      properties:
        category:
          type: string
          maxLength: 80

    BattleTopicSuggestions:
      type: object
      required: [topics]
      properties:
        topics:
          type: array
          items:
            type: string

    BattleTurnInput:
      type: object
      required: [content]
      properties:
        content:
          type: string
          minLength: 1
          maxLength: 1000

    BattleEvaluation:
      type: object
      required: [logic, persuasiveness, rebuttal, wit, manners, total, feedback, violation]
      properties:
        logic:
          type: integer
        persuasiveness:
          type: integer
        rebuttal:
          type: integer
        wit:
          type: integer
        manners:
          type: integer
        total:
          type: integer
        feedback:
          type: string
        violation:
          type: boolean

    BattleParticipant:
      type: object
      required: [userId, name, side, totalScore, ready]
      properties:
        userId:
          type: string
        name:
          type: string
        side:
          type: string
          description: "pro or con (empty until the game starts)"
        totalScore:
          type: integer
        ready:
          type: boolean
        isAI:
          type: boolean
          description: True when this participant is an AI persona opponent.
        personaId:
          type: string
          description: AI persona id when isAI is true.

    BattlePersona:
      type: object
      required: [id, name, emoji, tagline]
      properties:
        id:
          type: string
        name:
          type: string
        emoji:
          type: string
        tagline:
          type: string

    BattlePersonaList:
      type: object
      required: [personas]
      properties:
        personas:
          type: array
          items:
            $ref: "#/components/schemas/BattlePersona"

    BattleState:
      type: object
      required:
        [
          roomId,
          status,
          topic,
          category,
          startQuestion,
          phase,
          participants,
          totalRounds,
          round,
          timeLimitSeconds,
          turnIndex,
          ended,
        ]
      properties:
        roomId:
          type: string
        status:
          type: string
        hostUserId:
          type: ["string", "null"]
          description: The battle creator's userId; only the host may cancel while waiting.
        topic:
          type: string
        category:
          type: string
        startQuestion:
          type: string
        phase:
          type: string
          description: "waiting | active | ended"
        participants:
          type: array
          items:
            $ref: "#/components/schemas/BattleParticipant"
        totalRounds:
          type: integer
        round:
          type: integer
          description: Current round (1-based); 0 before the game starts.
        timeLimitSeconds:
          type: integer
        turnIndex:
          type: integer
          description: 0-based turn index across the whole game; -1 before start.
        currentSpeakerUserId:
          type: ["string", "null"]
        turnStartedAt:
          type: ["string", "null"]
          description: ISO timestamp the current turn's clock started.
        remainingSeconds:
          type: integer
          description: Server-computed seconds left in the current turn (0 when no active turn).
        ended:
          type: boolean
        winnerUserId:
          type: ["string", "null"]
          description: Winner userId, or null for a draw / not yet ended.

    MyBattleStats:
      type: object
      required:
        [
          wins,
          losses,
          draws,
          total,
          winRate,
          currentStreak,
          bestStreak,
          mp,
          level,
          title,
          mpIntoLevel,
          mpForNextLevel,
          mpToNext,
        ]
      properties:
        wins:
          type: integer
        losses:
          type: integer
        draws:
          type: integer
        total:
          type: integer
        winRate:
          type: integer
          description: Win percentage (0-100), 0 when no games played.
        currentStreak:
          type: integer
        bestStreak:
          type: integer
        mp:
          type: integer
          description: Total accumulated mal-bbal points.
        level:
          type: integer
        title:
          type: string
        mpIntoLevel:
          type: integer
        mpForNextLevel:
          type: integer
        mpToNext:
          type: integer

    BattleHistoryPreview:
      type: object
      required: [name, content, isMe]
      properties:
        name:
          type: string
        content:
          type: string
        isMe:
          type: boolean

    BattleHistoryItem:
      type: object
      required:
        [roomId, topic, opponentName, myScore, opponentScore, outcome, comment, preview]
      properties:
        roomId:
          type: string
        topic:
          type: string
        opponentName:
          type: string
        opponentAvatarUrl:
          type: ["string", "null"]
        myScore:
          type: integer
        opponentScore:
          type: integer
        outcome:
          type: string
          enum: [win, loss, draw]
        comment:
          type: string
        preview:
          type: array
          items:
            $ref: "#/components/schemas/BattleHistoryPreview"

    ReadInput:
      type: object
      required: [messageId]
      properties:
        messageId:
          type: string

    MuteInput:
      type: object
      required: [muted]
      properties:
        muted:
          type: boolean

    Invite:
      type: object
      required: [id, inviteCode, inviterUserId, status, createdAt]
      properties:
        id:
          type: string
        inviteCode:
          type: string
          minLength: 8
          maxLength: 128
        inviterUserId:
          type: string
        status:
          type: string
        createdAt:
          type: string
        expiredAt:
          type: ["string", "null"]

    RedeemInviteInput:
      type: object
      required: [inviteCode]
      properties:
        inviteCode:
          type: string

    BlockInput:
      type: object
      required: [blockedUserId]
      properties:
        blockedUserId:
          type: string
          format: uuid

    CreateCallInput:
      type: object
      required: [calleeId]
      properties:
        calleeId:
          type: string
          format: uuid
        roomId:
          type: string
          format: uuid
        media:
          $ref: "#/components/schemas/CallMedia"

    CallMedia:
      type: string
      enum: [audio, video]

    CallStatus:
      type: string
      enum:
        [
          ringing,
          active,
          declined,
          missed,
          cancelled,
          ended,
          failed,
        ]

    Call:
      type: object
      required: [id, roomName, callerId, calleeId, media, status, createdAt]
      properties:
        id:
          type: string
        roomName:
          type: string
        callerId:
          type: string
        calleeId:
          type: string
        chatRoomId:
          type: ["string", "null"]
        media:
          $ref: "#/components/schemas/CallMedia"
        status:
          $ref: "#/components/schemas/CallStatus"
        createdAt:
          type: string
        acceptedAt:
          type: ["string", "null"]
        declinedAt:
          type: ["string", "null"]
        missedAt:
          type: ["string", "null"]
        cancelledAt:
          type: ["string", "null"]
        endedAt:
          type: ["string", "null"]
        durationSec:
          type: ["number", "null"]

    CallWithCaller:
      type: object
      required: [id, roomName, callerId, calleeId, media, status, createdAt, caller]
      properties:
        id:
          type: string
        roomName:
          type: string
        callerId:
          type: string
        calleeId:
          type: string
        chatRoomId:
          type: ["string", "null"]
        media:
          $ref: "#/components/schemas/CallMedia"
        status:
          $ref: "#/components/schemas/CallStatus"
        createdAt:
          type: string
        endedAt:
          type: ["string", "null"]
        caller:
          $ref: "#/components/schemas/PublicUser"

    CallSession:
      type: object
      required: [call, token, url]
      properties:
        call:
          $ref: "#/components/schemas/Call"
        token:
          type: string
        url:
          type: string

    ClanMember:
      type: object
      required:
        [
          userId,
          displayName,
          level,
          title,
          archetype,
          archetypeLabel,
          role,
          contributionExp,
        ]
      properties:
        userId:
          type: string
        displayName:
          type: string
        avatarUrl:
          type: ["string", "null"]
        level:
          type: integer
        title:
          type: string
        archetype:
          type: string
        archetypeLabel:
          type: string
        role:
          type: string
          enum: [owner, elder, member]
        contributionExp:
          type: integer

    Clan:
      type: object
      required: [id, name, level, exp, memberCount, createdAt]
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: ["string", "null"]
        emblemUrl:
          type: ["string", "null"]
        level:
          type: integer
        exp:
          type: integer
        memberCount:
          type: integer
        clanValues:
          type: ["string", "null"]
        clanSummary:
          type: ["string", "null"]
        preferredArchetype:
          type: ["string", "null"]
        createdAt:
          type: string

    ClanSummary:
      type: object
      required: [id, name, level, memberCount]
      properties:
        id:
          type: string
        name:
          type: string
        description:
          type: ["string", "null"]
        emblemUrl:
          type: ["string", "null"]
        level:
          type: integer
        memberCount:
          type: integer
        preferredArchetype:
          type: ["string", "null"]

    ClanDetail:
      allOf:
        - $ref: "#/components/schemas/Clan"
        - type: object
          required: [members]
          properties:
            members:
              type: array
              items:
                $ref: "#/components/schemas/ClanMember"

    MyClan:
      type: object
      required: [clan, myRole, memberCount, recentMembers]
      properties:
        clan:
          $ref: "#/components/schemas/Clan"
        myRole:
          type: string
          enum: [owner, elder, member]
        memberCount:
          type: integer
        recentMembers:
          type: array
          items:
            $ref: "#/components/schemas/ClanMember"

    ClanIdentity:
      type: object
      required:
        [
          clanId,
          level,
          exp,
          expIntoLevel,
          expForNextLevel,
          memberCount,
          averageLevel,
          clanPower,
          dominantArchetype,
          dominantArchetypeLabel,
          topStrengths,
        ]
      properties:
        clanId:
          type: string
        level:
          type: integer
        exp:
          type: integer
        expIntoLevel:
          type: integer
        expForNextLevel:
          type: integer
        memberCount:
          type: integer
        averageLevel:
          type: integer
        clanPower:
          type: integer
        dominantArchetype:
          type: string
        dominantArchetypeLabel:
          type: string
        topStrengths:
          type: array
          items:
            type: string

    ClanRankingItem:
      type: object
      required:
        [
          rank,
          clanId,
          name,
          emblemUrl,
          level,
          exp,
          memberCount,
          dominantArchetype,
          dominantArchetypeLabel,
          clanPower,
          averageLevel,
          topStrengths,
          score,
        ]
      properties:
        rank:
          type: integer
        clanId:
          type: string
        name:
          type: string
        emblemUrl:
          type: string
          nullable: true
        level:
          type: integer
        exp:
          type: integer
        memberCount:
          type: integer
        dominantArchetype:
          type: string
        dominantArchetypeLabel:
          type: string
        clanPower:
          type: integer
        averageLevel:
          type: integer
        topStrengths:
          type: array
          items:
            type: string
        score:
          type: integer

    ClanRankingMyRank:
      type: object
      required: [rank, score, pointsToNextRank]
      properties:
        rank:
          type: integer
        score:
          type: integer
        pointsToNextRank:
          type: integer

    ClanRanking:
      type: object
      required: [type, items]
      properties:
        type:
          type: string
          enum: [overall, level, contribution, average_level, archetype]
        archetype:
          type: string
          nullable: true
        items:
          type: array
          items:
            $ref: "#/components/schemas/ClanRankingItem"
        myClanRank:
          oneOf:
            - $ref: "#/components/schemas/ClanRankingMyRank"
            - type: "null"

    ClanCreate:
      type: object
      required: [name]
      properties:
        name:
          type: string
          minLength: 2
          maxLength: 20
        description:
          type: string
          maxLength: 300
        clanValues:
          type: string
          maxLength: 200
        preferredArchetype:
          type: string
          enum:
            [
              strategist,
              harmonizer,
              explorer,
              pioneer,
              sage,
              entertainer,
              activist,
              observer,
            ]
        emblemUrl:
          type: string

    ClanLeaveResult:
      type: object
      required: [clanDeleted]
      properties:
        clanDeleted:
          type: boolean

    ClanError:
      type: object
      required: [error, message]
      properties:
        error:
          type: string
        message:
          type: string

    ClanMemory:
      type: object
      required:
        [
          id,
          clanId,
          sourceType,
          memoryType,
          title,
          summary,
          importanceScore,
          tags,
          createdAt,
        ]
      properties:
        id:
          type: string
        clanId:
          type: string
        sourceType:
          type: string
          enum: [battle, dungeon, manual, system]
        sourceId:
          type: string
          nullable: true
        memoryType:
          type: string
          enum: [strategy, lesson, value, achievement, warning]
        title:
          type: string
        summary:
          type: string
        importanceScore:
          type: integer
        tags:
          type: array
          items:
            type: string
        createdByUserId:
          type: string
          nullable: true
        authorName:
          type: string
          nullable: true
        createdAt:
          type: string

    ClanMemoryList:
      type: object
      required: [items]
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ClanMemory"

    ClanMemoryCreate:
      type: object
      required: [memoryType, title, summary]
      properties:
        memoryType:
          type: string
          enum: [strategy, lesson, value, achievement, warning]
        title:
          type: string
          minLength: 1
          maxLength: 80
        summary:
          type: string
          minLength: 1
          maxLength: 1000
        tags:
          type: array
          maxItems: 5
          items:
            type: string
            maxLength: 20
        sourceType:
          type: string
          enum: [battle, dungeon, manual, system]
        sourceId:
          type: string
        sourceKey:
          type: string

    ClanMemoryDeleteResult:
      type: object
      required: [deleted]
      properties:
        deleted:
          type: boolean

    ClanWisdom:
      type: object
      required:
        [
          clanId,
          philosophy,
          strategy,
          values,
          culture,
          motto,
          sourceMemoryCount,
          generatedAt,
        ]
      properties:
        clanId:
          type: string
        philosophy:
          type: string
        strategy:
          type: string
        values:
          type: string
        culture:
          type: string
        motto:
          type: string
        sourceMemoryCount:
          type: integer
        generatedByUserId:
          type: string
          nullable: true
        generatedByName:
          type: string
          nullable: true
        generatedAt:
          type: string

    ClanWarSummary:
      type: object
      required:
        [
          id,
          topic,
          status,
          challengerClanId,
          challengerScore,
          opponentScore,
          participantCount,
          createdAt,
        ]
      properties:
        id:
          type: string
        topic:
          type: string
        status:
          type: string
          enum: [open, matched, active, completing, completed, cancelled]
        challengerClanId:
          type: string
        challengerClanName:
          type: string
          nullable: true
        opponentClanId:
          type: string
          nullable: true
        opponentClanName:
          type: string
          nullable: true
        winnerClanId:
          type: string
          nullable: true
        challengerScore:
          type: integer
        opponentScore:
          type: integer
        participantCount:
          type: integer
        createdAt:
          type: string

    ClanWarParticipant:
      type: object
      required: [userId, side, hasSubmitted, score]
      properties:
        userId:
          type: string
        displayName:
          type: string
          nullable: true
        side:
          type: string
          enum: [challenger, opponent]
        hasSubmitted:
          type: boolean
        score:
          type: integer
        contributionSummary:
          type: string
          nullable: true

    ClanWarResult:
      type: object
      required: [judgeSummary, challengerFeedback, opponentFeedback]
      properties:
        judgeSummary:
          type: string
        challengerFeedback:
          type: string
        opponentFeedback:
          type: string

    ClanWarDetail:
      allOf:
        - $ref: "#/components/schemas/ClanWarSummary"
        - type: object
          required: [participants, myHasJoined]
          properties:
            participants:
              type: array
              items:
                $ref: "#/components/schemas/ClanWarParticipant"
            mySubmission:
              type: string
              nullable: true
            mySide:
              type: string
              enum: [challenger, opponent]
              nullable: true
            myHasJoined:
              type: boolean
            result:
              oneOf:
                - $ref: "#/components/schemas/ClanWarResult"
                - type: "null"
    ProfilePostsPage:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/StarFeedPost" }
        nextCursor:
          type: string
          format: date-time
          nullable: true
    GrowthRecordsPage:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/GrowthRecord" }
        nextCursor: { type: string, format: date-time, nullable: true }
    GrowthRecord:
      type: object
      required: [id, starProfileId, eventType, xpDelta, createdAt]
      properties:
        id: { type: string, format: uuid }
        starProfileId: { type: string, format: uuid }
        eventType: { type: string }
        xpDelta: { type: integer }
        reason: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
    BattleResultsPage:
      type: object
      required: [items, nextCursor]
      properties:
        items:
          type: array
          items: { $ref: "#/components/schemas/PublicBattleResult" }
        nextCursor: { type: string, format: date-time, nullable: true }
    PublicBattleResult:
      type: object
      required: [roomId, topic, category, outcome, myScore, opponentScore, opponentName, completedAt]
      properties:
        roomId: { type: string, format: uuid }
        topic: { type: string }
        category: { type: string }
        outcome: { type: string, enum: [win, loss, draw] }
        myScore: { type: integer }
        opponentScore: { type: integer }
        opponentName: { type: string }
        completedAt: { type: string, format: date-time }
    PublicCharacterProfileResponse:
      type: object
      required: [profile, posts]
      properties:
        profile:
          allOf:
            - $ref: "#/components/schemas/CharacterProfileSummary"
            - type: object
              required: [level, xp, jobStage, jobLabel, stats, metadata, isMine, followedByMe, followerCount, followingCount, postCount]
              properties:
                statusMessage: { type: ["string", "null"] }
                level: { type: integer, minimum: 1 }
                xp: { type: integer, minimum: 0 }
                jobKey: { type: ["string", "null"] }
                jobStage: { type: integer, minimum: 0 }
                jobLabel: { type: string }
                characterImageUrl: { type: ["string", "null"] }
                stats: { type: object, additionalProperties: { type: number } }
                metadata: { type: object, additionalProperties: true }
                isMine: { type: boolean }
                followedByMe: { type: boolean }
                followerCount: { type: integer, minimum: 0 }
                followingCount: { type: integer, minimum: 0 }
                postCount: { type: integer, minimum: 0 }
        posts:
          type: object
          required: [items, nextCursor]
          properties:
            items: { type: array, items: { $ref: "#/components/schemas/StarFeedPost" } }
            nextCursor: { type: ["string", "null"], format: date-time }

    CharacterProfileFollowState:
      type: object
      required: [following]
      properties:
        following: { type: boolean }

    PublicProfile:
      type: object
      required: [id, nickname, stars, followerCount, followingCount]
      properties:
        id: { type: string, format: uuid }
        nickname: { type: string }
        profileImageUrl: { type: string, nullable: true }
        statusMessage: { type: string, nullable: true }
        createdAt: { type: string, format: date-time }
        fan: { type: object }
        stars: { type: array, items: { type: object } }
        followerCount: { type: integer }
        followingCount: { type: integer }
        followedStarIds: { type: array, items: { type: string, format: uuid } }
