---
name: EAS Build pnpm lockfile compatibility
description: Why EAS Android/iOS builds fail at "Install dependencies" with a valid lockfile, and the corepack + packageManager fix.
---

# EAS Build ignores a newer-format pnpm lockfile

EAS Build runs `pnpm install --frozen-lockfile` in CI. If the repo's `pnpm-lock.yaml`
was generated by a newer pnpm than EAS's bundled default, EAS logs
`WARN Ignoring not compatible lockfile` then `ERR_PNPM_NO_LOCKFILE ... lockfile is absent`
and the build fails at the **Install dependencies** phase — even though the lockfile is
present and `pnpm install --frozen-lockfile` passes locally.

**Why:** `lockfileVersion: '9.0'` requires pnpm >= 9. EAS's default pnpm can be older, so
it cannot parse the lockfile, treats it as missing, and frozen-install aborts.

**How to apply / fix (both are required):**
1. Pin the package manager in **root** `package.json`: `"packageManager": "pnpm@<version>"`
   (match the local pnpm that wrote the lockfile, e.g. `pnpm@10.26.1`).
2. Set `"corepack": true` on every build profile in `artifacts/mobile/eas.json`
   (development / preview / production). Without corepack enabled, EAS ignores the
   `packageManager` field and keeps using its bundled pnpm.

**Watch for a different but related EAS install failure:** `ERR_PNPM_OUTDATED_LOCKFILE
... specifiers in the lockfile don't match package.json`. That one means the *uploaded*
working tree is stale (e.g. a half-finished `pnpm add` left package.json and lockfile
out of sync on the user's local clone). Fix is on their machine: `git fetch && git reset
--hard origin/main` to take the repo's synced state, not a code change here.
