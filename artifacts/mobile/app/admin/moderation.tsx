import React from "react";
import { ActivityIndicator, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { Feather } from "@expo/vector-icons";
import { useColors } from "@/hooks/useColors";
import { useAdminModeration } from "@/hooks/useAdminModeration";
export default function AdminModerationScreen() { const colors = useColors(); const { data = [], isLoading, refetch, resolve } = useAdminModeration(); return <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={styles.container}><Text style={[styles.title, { color: colors.foreground }]}>신고·제재</Text><Text style={[styles.subtitle, { color: colors.mutedForeground }]}>피드 신고를 검토하고 게시물 조치를 처리합니다.</Text>{isLoading ? <ActivityIndicator color={colors.primary} /> : data.length === 0 ? <View style={styles.empty}><Feather name="check-circle" size={26} color={colors.primary} /><Text style={{ color: colors.foreground }}>처리할 신고가 없습니다.</Text><Pressable onPress={() => void refetch()}><Text style={{ color: colors.primary }}>새로고침</Text></Pressable></View> : data.map((report) => <View key={report.id} style={[styles.card, { backgroundColor: colors.card, borderColor: colors.border }]}><Text style={[styles.reason, { color: colors.foreground }]}>{report.reason}</Text><Text style={{ color: colors.mutedForeground }}>{report.postTitle ?? "게시물"} · 신고자 {report.reporterNickname ?? "알 수 없음"}</Text>{report.details ? <Text style={{ color: colors.mutedForeground }}>{report.details}</Text> : null}<View style={styles.actions}><Pressable onPress={() => void resolve.mutateAsync({ id: report.id, action: "keep" })} style={[styles.button, { backgroundColor: colors.secondary }]}><Text style={{ color: colors.foreground }}>유지</Text></Pressable><Pressable onPress={() => void resolve.mutateAsync({ id: report.id, action: "remove" })} style={[styles.button, { backgroundColor: colors.primary }]}><Text style={{ color: colors.primaryForeground }}>게시물 숨김</Text></Pressable></View></View>)}</ScrollView>; }
const styles = StyleSheet.create({ container: { padding: 18, gap: 12 }, title: { fontSize: 23, fontWeight: "800" }, subtitle: { fontSize: 12 }, card: { borderWidth: 1, borderRadius: 16, padding: 14, gap: 8 }, reason: { fontSize: 15, fontWeight: "700" }, actions: { flexDirection: "row", gap: 8 }, button: { borderRadius: 9, paddingHorizontal: 12, paddingVertical: 9 }, empty: { minHeight: 160, alignItems: "center", justifyContent: "center", gap: 10 } });
