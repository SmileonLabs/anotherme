import React from "react";
import { ActivityIndicator, ScrollView, StyleSheet, Text, View } from "react-native";
import { useColors } from "@/hooks/useColors";
import { useAdminOperations } from "@/hooks/useAdminOperations";
const labels: Array<[keyof NonNullable<ReturnType<typeof useAdminOperations>["data"]>, string]> = [["posts", "피드 게시물"], ["reports", "신고"], ["rooms", "채팅방"], ["calls", "통화"], ["searches", "검색 요청"]];
export default function AdminOperationsScreen() { const colors = useColors(); const { data, isLoading } = useAdminOperations(); return <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={styles.container}><Text style={[styles.title, { color: colors.foreground }]}>운영 현황</Text><Text style={[styles.subtitle, { color: colors.mutedForeground }]}>피드·검색·채팅·통화의 현재 누적 상태입니다.</Text>{isLoading ? <ActivityIndicator color={colors.primary} /> : <View style={styles.grid}>{labels.map(([key, label]) => <View key={key} style={[styles.card, { backgroundColor: colors.card, borderColor: colors.border }]}><Text style={[styles.label, { color: colors.mutedForeground }]}>{label}</Text><Text style={[styles.value, { color: colors.foreground }]}>{data?.[key] ?? 0}</Text></View>)}</View>}</ScrollView>; }
const styles = StyleSheet.create({ container: { padding: 18, gap: 12 }, title: { fontSize: 23, fontWeight: "800" }, subtitle: { fontSize: 12 }, grid: { flexDirection: "row", flexWrap: "wrap", gap: 10 }, card: { width: "47%", borderWidth: 1, borderRadius: 14, padding: 14, gap: 8 }, label: { fontSize: 12 }, value: { fontSize: 25, fontWeight: "800" } });
